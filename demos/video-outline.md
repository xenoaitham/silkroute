# 10-minute demo video outline

Maple Retail Group is a fictional company; SilkRoute is a self-directed reference implementation (2026).

Format: screen recording, terminal plus browser (GitHub). Every on-screen artifact is a real repo file under `evidence/runs/` or a live command run on camera — no slides except the opening label. Rule for the whole recording: every number said aloud appears on screen at the same time, and every cloud IaC moment carries the "validated IaC, sim runtime" line.

| # | Time | Section | On screen | Say (key lines) | Evidence artifact flashed |
|---|---|---|---|---|---|
| 1 | 00:00–00:40 | Opening + honest label | Title card (plain text overlay on a terminal): "SilkRoute — Multi-Region Alibaba Cloud Integration Platform. Maple Retail Group is a FICTIONAL company. Self-directed reference implementation, 2026. Cloud IaC is validated-plans; runtime evidence is sim mode." Then `cat docs/scenario-charter.md` constraint table | "I built a reference implementation of exactly the problem your JD describes — a Canadian retailer's APAC expansion on AliCloud. The retailer is fictional by design; the code and the numbers are real, and every number has a reproduce command." | `docs/scenario-charter.md` (C1–C7) |
| 2 | 00:40–01:50 | Sim stack from clean | Full-screen terminal: `make down && make up && make ps && make smoke` — show convergence and `smoke OK (all 6 services)` | "Dual-mode by design: five pinned services — MySQL 8, Kafka 3.8 KRaft, Redis 7, MinIO, toxiproxy — plus a dedicated ERP fault-injection proxy, mirroring the managed interfaces. Converges in about 28 seconds, and the smoke is falsifiable." | `evidence/runs/E-001-compose-ps.txt`, `E-002-smoke.txt`, `E-015-s5-reverification.txt` |
| 3 | 01:50–02:40 | Smoke lies? Prove it can't | Terminal: `docker compose stop redis && make smoke` → `FAIL: redis host forward 127.0.0.1:16379 not reachable`, exit 2; `docker compose start redis && make smoke` → exit 0 | "A check you haven't seen fail isn't a check. Here's the negative control, on camera." | `evidence/runs/E-003-smoke-negative-control.txt` |
| 4 | 02:40–04:10 | The order saga live | Terminal: `make esb-run`; `scripts/demo-order.sh` (the README quickstart path) → show 201 JSON (CAD 1000 minor = 2 × 500, saga steps, attempts, route); replay the SAME key: `scripts/demo-order.sh demo-409` → 409 DUPLICATE with original orderId | "One order, end to end: REST → canonical JSON Schema → idempotency in Redis → SOAP saga order, reserve, pricing, confirm over WS-Security UsernameToken, XSLT on both legs. Replay the key and you get a 409 carrying the original order — never a double order." | `evidence/runs/E-008-esb-build-and-saga.txt`, `E-027-stranger-walk.txt` |
| 5 | 04:10–05:50 | Resilience under fault injection | Terminal: `bash tests/chaos/esb-faults.sh latency 2500` then `scripts/demo-order.sh demo-retry` (show attempts=3 on the exhausted saga); `disable` (hard-down) → 503 UPSTREAM-UNAVAILABLE bounded (~0.6 s), rapid re-posts → 503 CIRCUIT-OPEN fail-fast (4 ms in the E-009 proof); `clean` → 201 again. Then `ESB_FAULT_INJECTION` fail-pricing run → 503 SAGA-COMPENSATED; quick ERP stock query showing 19→19; DLQ event JSON showing `compensated:true` and `msk-…` customerRef | "Real fault injection through toxiproxy: the retry ladder runs three attempts then fails bounded; hard-down drives the breaker open — fail-fast in 4 milliseconds in the recorded proof — and the DLQ carries the exhausted correlation IDs. A failed saga really releases its holds: stock 19 before, 19 after. And note the dead-letter event: the Chinese customer reference is pseudonymized before it ever leaves the region's application layer." | `evidence/runs/E-009-esb-int-suite.txt` |
| 6 | 05:50–06:50 | Measured latency (C3) | Terminal: `make load-orders`; k6 summary printed on screen — highlight p95, checks; `tests/load/results/k6-orders-summary.json` open | "C3 is a p95 budget of 300 milliseconds; measured: 44.25 ms at ~19 RPS over 1,304 iterations, 98.61% checks — and every non-201 is an out-of-stock business outcome, zero infra failures. No thresholds in the k6 script: it records the number, it doesn't pass itself." | `evidence/runs/E-010-k6-run.txt`, `E-010-k6-summary.json` |
| 7 | 06:50–08:20 | AliCloud landing zone (validated IaC) | Terminal: `make plan-sg` — show fmt/validate/lint OK and `Plan: 87 to add`; then `terraform plan -var enable_cn_region=true` → `Plan: 118 to add`; grep planned values for `cn-beijing-*`. Browser: GitHub Actions page, 4 green jobs (run 34643255075) | "Now the cloud side — and the honest label: validated IaC, sim runtime. Plans are proven against the real pinned provider and perform zero API calls; nothing is ever applied because no account exists. Singapore hub, 87 resources including the SLS alert layer; with the China flag on, 118 — and the CN partition is pinned at the provider graph, the aliased alicloud.cn provider, not by tags. CI proves it on every push: four green jobs, including the fault-injection suite booting both services on a hosted runner." | `evidence/runs/E-012-tf-validate-plan.txt`, `E-012-first-terraform-ci-run.txt` |
| 8 | 08:20–09:10 | Honesty mechanisms | Terminal: `bash scripts/budget-alarm.sh` (dry-run payload printed, no call); `make deploy-sg` → exit 2 REFUSING; `make destroy` → exit 2 REFUSING; `bash scripts/tf-apply-validity.sh` → OK | "Three honesty mechanisms worth their own ten seconds. The budget alarm is a script, because the provider genuinely has no budget resource — schema-proven — targeting the doc-verified BSS CreateBudget API, dry-run by default. The billable targets refuse to run without explicit confirmation. And the apply-validity lint guards constraints plans can't see, like ApsaraMQ's topic-naming rule — the war story that taught me a plan proves schema, not apply." | `evidence/runs/E-014-budget-alarm-and-guards.txt` |
| 9 | 09:10–09:45 | Compliance & what's next | Split screen: `infra/cn-partition/` tags (`residency=cn`, `data-classification=pipl-restricted`) and `apps/esb` PiiMaskingPolicy javadoc; then `make residency` output (7/7 + selftest); then roadmap text: "next: runtime residency proof at activation; ETL (Debezium → Kafka → bronze/silver/gold) designed, NOT built" | "Residency posture, stated precisely: CN PII designed to never leave the CN partition — separate region, no replication, no egress path, masked egress from shared topics. The static residency checks run in CI on every push, with a selftest that injects five violations to prove they bite; runtime proof waits for an account. And the ETL phase is designed, not built — I quote no numbers for it." | `evidence/runs/E-016-residency-tests.txt`, `compliance/iam-review-phase4.md` (§5 residency), `decisions/ADR-0005-region-strategy-sg-hub-cn-partition.md` |
| 10 | 09:45–10:00 | Close | Title card: repo URL + "Every claim → evidence row → reproduce command. Maple Retail Group is fictional; the measurements are not." | "Everything you saw has a reproduce command in the evidence ledger — the README quickstart itself has been walked start-to-finish from a clean clone. Happy to go a level deeper on any of it." | `evidence/EVIDENCE.md` (ledger scroll), `docs/hld.md` + `docs/runbooks.md` flash |

## Recording notes

- **Do not** show real credentials at any step; the WSS dummies (esb-client / erp-wss-pass-2026) are documented sim dummies and may appear only inside config files already in the repo.
- Sections 4–6 need the stack from section 2 running: `make up` + `make smoke` exit 0 before recording; `./scripts/esb-toxiproxy.sh ensure` before section 5; `make esb-stop` after section 6.
- Section 7 runs offline by design: `make plan-sg` performs no AliCloud API calls. Say the "validated IaC, sim runtime" line on camera — it must be audible, not just visible.
- Keep each evidence-artifact flash to 2–4 seconds; the filename must be readable.
- If a live command misbehaves on camera, cut and re-record that section — never narrate over a failure with "usually works".
