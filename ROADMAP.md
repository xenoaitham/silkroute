# SILKROUTE ROADMAP — Phase Status Board

Legend: `TODO` → not started · `IN_PROGRESS` → in flight · `REVIEW` → built, awaiting Critic Gate · `DONE` → Critic PASS recorded · `DE-SCOPED` → honestly cut (ADR required)

Fast path (MVCP): 0 → 1 → 2 → 4-lite → 8. Phases 3, 5, 6, 7 amplify depth; Phase 5 is what makes the Asia-rollout story credible.

| Phase | Goal | Status | Key evidence IDs | Notes |
|---|---|---|---|---|
| 0 — Foundation | Scaffolding, state machinery, sim/cloud decision, compose skeleton starts | **DONE** (S2) | E-001, E-002, E-003 | cycle 3 **PASS 8.50** (fresh critic, live negative-path re-verification); boundary-exact — 1 med + 3 low must-fixes applied in S2 (host-forward TCP probes in smoke, E-003 re-captured w/ stderr, E-001 command bound to probe). CI-on-Actions discharged 2026-09-10 → E-007 |
| 1 — Legacy ERP (SOAP, WSDL-first) | Untouchable legacy estate: WSDLs + XSDs first, Spring Boot + CXF, WS-Security, typed faults, seed data, Karate SOAP contract tests, WSDL freeze (C6) | **DONE** (S2) | E-004, E-005, E-006 | gate cycle 1 **PASS 8.85** (fresh critic: independent builds, wire probes, dead-port falsifiability, freeze-tag diff); freeze = ADR-0003 + tag `contract-freeze-erp-v1`; contract suite green on GitHub Actions (E-007, run 34525438436); 2 low follow-ups → S3 start |
| 2 — ESB core | Camel hub: REST→canonical→SOAP saga w/ compensation, XSLT, CBR by store region, retry/CB/DLQ, idempotent consumer, PII-masking egress; toxiproxy fault tests; measured p95 (C3) | **DONE** (S3) | E-008, E-009, E-010, E-011 | apps/esb on Camel 4.10.3 LTS + Boot 3.4.5, CXF client from the frozen WSDLs; 33 unit tests; 8/8 fault-injection scenarios (retry attempts=3/2, CIRCUIT-OPEN 4ms + DLQ + recovery, compensation stock 19→19, CN DLQ masking, idempotency-release regression); k6 p95 **44.25ms** @ 18.6 RPS; CI 3/3 jobs green (E-011). Gate cycle 2 **PASS 8.50** |
| 3 — ETL pipelines | Debezium→Kafka→bronze; Spark silver/gold; 100% reconciliation; DQ quarantine; freshness ≤15min (C4), T+1 by 06:00 SGT (C5) | TODO | — | |
| 4 — AliCloud IaC & cloud deploy | Terraform alicloud modules, remote state, validate+plan clean, RAM least-privilege, budget alarm; deploy or validated-plans mode per ADR-0002 | **REVIEW** (S4) | E-012, E-013, E-014 | Validated-plans mode (ADR-0002): alicloud 1.285.0 pinned, tf 1.16.2; 6 modules + SG root — Plan **75 add** SG / **106 add** CN-flag (designed, never applied, provider-level region pinning per SEC-4-01 fix); SEC-1 FIX-FIRST → all must-fixes + actionable lows closed same session; budget alarm = schema-gap-honest BSSOpenAPI script + double-guarded deploy targets; `make plan-sg` one-command AC proven; CI 4th job added. ADR-0004 (SAE) + ADR-0005 (region strategy). Critic gate PENDING |
| 5 — Compliance & security | CI-provable residency tests (C1), PIPL/PDPA/PIPEDA matrix w/ article citations, cross-border memo (PIPL 38–40), ICP runbook, STRIDE, ActionTrail→SLS demo | TODO | — | Asia-rollout credibility layer |
| 6 — Observability, load, chaos, cost | SLS dashboards/alerts/SLOs, k6 measured numbers, chaos runbook executed, cost sheet from real pricing pages | TODO | — | |
| 7 — Docs & demo | Stranger-testable README, HLD/LLD, ADR set, runbooks, delivery model (RACI), demo video | TODO | — | |
| 8 — Resume & interview pack | Resume bullets w/ evidence IDs, 40-Q deep-drill Q&A, 25-min whiteboard script, final gate: 3-round hostile mock interview | TODO | — | Final gate = project complete |

## Critic Gate log

| Date | Phase | Cycle | Weighted score | Verdict | Findings summary |
|---|---|---|---|---|---|
| 2026-09-08 | 0 | 1 | 7.80 | **FAIL** | 1 high (E-002 row not bound to its command/artifact) + 6 med (§3 tree not in git, STATE over-claims, smoke 2/5, hardcoded creds/0.0.0.0 binds, undeclared jq dep, CI never executed) + 5 low. All fixed. |
| 2026-09-08 | 0 | 2 | 7.75 | **FAIL** | 1 crit proven live (`make smoke` could not fail — `set -e` exempts non-final `&&`-list members), 1 high (hardcoded `-psilkroute` broke credential indirection), 2 med (kafka host listener unusable, README port omissions), 2 low (curl undeclared, CI re-implements wait). All fixed incl. negative-control evidence E-003. |
| 2026-09-09 | 0 | 3 | **PENDING** | **OPEN** | Critic subagent spawn failed twice (quota limit / model request failed) — no fresh-agent verdict exists. ORCH-LEAD self-audit executed instead (secrets scan clean; compose config OK; negative path re-proven: smoke exit 2 down / 0 restored; git clean; §3 tree in git; stack recovered after daemon restart). **Phase 0 stays REVIEW. Fresh CRITIC required at S2 start before Phase 1 work.** |
| 2026-09-09 | 0 | 3 (S2) | **8.50** | **PASS** | Fresh critic verified all cycle-1/2 fixes live, ran the negative path itself (stop redis → smoke exit 2 → restore → exit 0). Boundary-exact pass: 1 med (smoke lacked host-side forward probes for 16379/3306/39092 — the rootlesskit failure mode) + 3 low (E-003 artifact missing FAIL line/stderr, E-001 probe not bound to reproduce command, mysql exec failure path aborts without message). All four fixed in S2 before Phase 1 build work; smoke re-proven (exit 2 → 0) post-fix. Standing: first green GitHub Actions run becomes an evidence row once a remote exists. |
| 2026-09-09 | 1 | 1 | **8.85** | **PASS** | Fresh critic re-ran builds (15 unit + 16 contract), 7/7 own wire probes (incl. BSP-strict WSS with correct profile-URI Password Type + replay-cache rejection), proved falsifiability live (dead-port ERP_TEST_BASEURL → red), verified freeze mechanics (tag `contract-freeze-erp-v1`, empty wsdl/xsd diff), reproduced E-004's negative enum case. Zero crit/high. 2 med + 4 low non-blocking; 4 closed in S2 (Receiver semantics → ADR-0003; E-006 probe → scripts/erp-verify.sh + row rebound; CI drop -DskipTests; STATE root-pom strike). 2 deferred to S3 start: cross-endpoint auth + nonce-replay scenarios in security.feature; xsi:nil for nillable invalidValue (ErpFaults). |
| — | 2026-09-11 | 2 | 1 | 8.10 | **FAIL** | Fresh critic independently re-ran everything (freeze diff, builds, 8/8 suite, live probes incl. a reproduced defect). 1 high: FAILED sagas never released the Redis idempotency claim → same-key retry locked out 24h (reproduced live). 3 med: E-009 artifact mixed 3 runs (not 1:1); masking was unkeyed truncated SHA-256 overstated as non-reidentifying (now keyed HMAC pseudonymization); compensation releases rode the breaker they needed bypassed. 5 low (comments/docs/wording). ALL fixed same session; single-run artifacts re-captured; re-gate = cycle 2. |
| 2026-09-11 | 2 | 2 | **8.50** | **PASS** | Fresh critic re-ran everything (33/33 unit, 8/8 suite, freeze diff 0 bytes, CI runs, own live probes incl. reproducing the cycle-1 high on a path the suite doesn't cover and confirming the fix). Zero crit/high. 1 med (smoke/README coverage drift for esb-toxiproxy 18474 — closed in the S3 wrap-up: smoke now 6 services, exit 0 re-proven) + 3 low (provenance sentence corrected; masking-secret config row added; residual Redis-blip/crash lockout windows recorded as S4 follow-up). Phase 2 DONE. |
| 2026-09-11 | 4 | 1 | 7.21 | **FAIL** | Fresh critic reproduced all evidence live (plans 75/106 at its review time, guards exit 2, budget dry-run, wildcard greps, CI 4/4) and confirmed all SEC-1 fixes real — then found what validated-plans mode structurally hides: 1 **crit** (ApsaraMQ for Kafka CreateTopic **rejects dots in topic names** — the "sim byte-identical topic names" parity claim was an apply-time impossibility plan couldn't see), 2 **high** (budget script cited an uncitable `SetBudgets` API — retargeted to doc-verified `CreateBudget` 2023-09-30; egress-isolation claims prose-only, basic SGs are default-ALLOW egress + missing snat_entries), 5 med (Redis backend unmodeled, RDS TDE-key grant residue, dead private_2, stale 73/104 numbers, evidence uncommitted) + 4 low. ALL fixed same session: env-indirected topic names (`KAFKA_ORDERS_TOPIC`/`KAFKA_DLQ_TOPIC`, sim defaults unchanged, esb unit 33/33), new `scripts/tf-apply-validity.sh` lint in CI (negative control proven), snat_entries + honest egress comments, KMS trim to OSS key, data→private_2, doc corrections + SEC-1 review addendum. Plans re-proven 79/110 → cycle 2. |
| 2026-09-11 | 4 | 2 | 8.36 | **FAIL** (0 crit/high) | Fresh critic verified all cycle-1 fixes real (topic chain end-to-end, CreateBudget vs live doc, egress comments + snat entries, lint negative control) and re-proved everything locally (79/110 plans, 33/33 esb unit, guards exit 2). Kept below the line by fix-commit hygiene: CI at HEAD red twice (transient Docker Hub minio-pull denial, unrecorded), a false `alicloud_sls_alert` schema claim left in ADR-0004/STATE, 4 stale 75/106 numbers (one ADR edited into staleness in its own fix commit), and an apply-ordering race on the new snat_entries. ALL fixed: retry wrapper on CI `make up` + flake recorded in E-012, schema claim corrected, numbers refreshed, `depends_on=[eip_association.nat]`, lint hardened against variable-RHS topics (negative control re-proven). → cycle 3. |
| — | — | — | — | — |

## De-scope log

| Date | Phase | What was cut | ADR |
|---|---|---|---|
| — | — | — | — |
