<?xml version="1.0" encoding="UTF-8"?>
<!--
  SILKROUTE the data plane - Spark bronze→silver→gold batch (apps/batch).

  the component spec component 3: bronze (raw, immutable, region-tagged) → Spark
  batch silver (cleansed/conformed) → gold star schema (fact_orders,
  fact_order_lines, dim_sku, dim_store, dim_date) with reconciliation totals
  (source vs gold, must be 100%) and DQ rules (completeness, uniqueness,
  referential) with a quarantine table; freshness/window metrics (C4/C5).

  - Spark 3.5.1 (Scala 2.13 artifacts), local[*] (SPARK_MASTER override), UI
    disabled, ALL binds loopback (shared sim host: zero new listening ports).
  - s3a against ${S3_ENDPOINT} (MinIO in sim) with path-style access; buckets
    mirror the IaC family names (ADR-0006 decision 3).
  - Money discipline (C5): every monetary column is BIGINT minor units + a
    currency STRING - FloatType/DoubleType appear NOWHERE in this module; the
    only floating-point-ish concept (freshness seconds) is emitted as integers.
  - DQ predicates are pure-Java (RowView) - unit-testable and reusable by
    selftest WITHOUT Spark; the Spark layer only adapts rows (see dq package).
  - CLI: full | recon-only | selftest (optional business-date argument).
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.mapleretail.silkroute</groupId>
  <artifactId>batch</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>jar</packaging>
  <name>batch</name>
  <description>Maple Retail (fictional) Spark bronze/silver/gold + DQ + reconciliation (the data plane)</description>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spark.version>3.5.1</spark.version>
    <scala.binary.version>2.13</scala.binary.version>
    <hadoop-aws.version>3.3.4</hadoop-aws.version>
    <aws.sdk.bundle.version>1.12.262</aws.sdk.bundle.version>
    <mysql.version>8.4.0</mysql.version>
    <jackson.version>2.15.2</jackson.version>
    <slf4j.version>2.0.13</slf4j.version>
    <junit.version>5.10.2</junit.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.apache.spark</groupId>
      <artifactId>spark-core_${scala.binary.version}</artifactId>
      <version>${spark.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.spark</groupId>
      <artifactId>spark-sql_${scala.binary.version}</artifactId>
      <version>${spark.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-aws</artifactId>
      <version>${hadoop-aws.version}</version>
    </dependency>
    <dependency>
      <groupId>com.amazonaws</groupId>
      <artifactId>aws-java-sdk-bundle</artifactId>
      <version>${aws.sdk.bundle.version}</version>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <version>${mysql.version}</version>
    </dependency>

    <!-- pin jackson ABOVE the aws bundle's older copy so Spark keeps its expected version -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.version}</version>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-core</artifactId>
      <version>${jackson.version}</version>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-annotations</artifactId>
      <version>${jackson.version}</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>${slf4j.version}</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-nop</artifactId>
      <version>${slf4j.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>batch-1.0.0-SNAPSHOT</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.6.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals><goal>shade</goal></goals>
            <configuration>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>com.mapleretail.silkroute.batch.BatchMain</mainClass>
                  <!-- Spark on JDK 17 via `java -jar`: JEP 261 manifest Add-Opens
                       replaces the spark-submit add-opens list. -->
                  <manifestEntries>
                    <Main-Class>com.mapleretail.silkroute.batch.BatchMain</Main-Class>
                    <Add-Opens>java.base/java.lang java.base/java.lang.invoke java.base/java.lang.reflect java.base/java.io java.base/java.net java.base/java.nio java.base/java.util java.base/java.util.concurrent java.base/java.util.concurrent.atomic java.base/sun.nio.ch java.base/sun.nio.cs java.base/sun.security.action java.base/sun.util.calendar</Add-Opens>
                  </manifestEntries>
                </transformer>
                <!-- THE classic fat-jar failure for Spark local jobs: service
                     files (META-INF/services/*) must be MERGED, not first-wins. -->
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
              </transformers>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                    <exclude>module-info.class</exclude>
                    <exclude>META-INF/versions/**/module-info.class</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
