<?xml version="1.0" encoding="UTF-8"?>
<!--
  SILKROUTE the data plane - Maple Retail Group (fictional) modern OMS event store
  (apps/modern-oms): the CDC SOURCE of the ETL data plane.

  Consumes ESB success events (topic silkroute.orders.events) into MySQL
  database silkroute_oms (oms_order + oms_order_line) with at-least-once
  semantics + a business-key idempotency guard (unique source_system +
  external_order_ref; INSERT ... ON DUPLICATE KEY UPDATE is a deliberate no-op)
  so replays never double-insert (ADR-0006 decision 2).

  Money discipline (C5): integer minor units + CAD/SGD/CNY code everywhere,
  integer multiplication for line totals - NEVER float/double. PII residency
  (C1): customerRef is stored VERBATIM as received on the event copy - it is
  ALREADY the ESB-masked value (msk-*) for CN stores; nothing here can unmask.

  No HTTP port (web-application-type=none): the shared sim host must gain zero
  new listening sockets. Every knob is ${VAR:default} (ADR-0001).

  Fat-jar note: built with spring-boot-maven-plugin (same style as apps/esb).
  Plain maven-shade would silently drop duplicate
  META-INF/spring/...AutoConfiguration.imports files (one per autoconfigure
  module), losing e.g. KafkaAutoConfiguration - the Boot repackage is the
  correct fat jar for a Spring app and java -jar works identically.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.4.5</version>
    <relativePath/>
  </parent>

  <groupId>com.mapleretail.silkroute</groupId>
  <artifactId>oms</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <name>oms</name>
  <description>Maple Retail (fictional) modern OMS: ESB success-event store, the CDC source (the data plane)</description>

  <properties>
    <java.version>17</java.version>
    <maven.compiler.release>17</maven.compiler.release>
  </properties>

  <dependencies>
    <!-- Core (logging, YAML config) - NO web starter: zero listening ports. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-json</artifactId>
    </dependency>
    <!-- Kafka consumer (manual-ack container, at-least-once). -->
    <dependency>
      <groupId>org.springframework.kafka</groupId>
      <artifactId>spring-kafka</artifactId>
    </dependency>
    <!-- Plain JDBC persistence (no JPA): DataSource + TransactionTemplate. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.kafka</groupId>
      <artifactId>spring-kafka-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <mainClass>com.mapleretail.silkroute.oms.OmsApplication</mainClass>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
