# SILKROUTE STATE

current_phase: 6 REVIEW (S7 executed the full ops-evidence slice; gate cycle 1 FAIL 8.50 fixed, cycle 2 in flight 2026-09-13) — phases 3/7 remain TODO (do not imply complete)
next_actions:
  1. Phase 6 re-gate (cycle 2, fresh critic) on the fixed tree; on PASS mark Phase 6 DONE with the honest note (SLS runtime + real cloud costs still need an account: designed + plan-validated + sim-measured is the claim — nothing stronger).
  2. Next build phase, owner's call: Phase 3 ETL (the pack's Q31–Q34 are design-only until it lands; the Phase-6 freshness panel + pipeline-metrics producer contract is now the documented receiving end) — or Phase 7 docs & demo. Phase 5's residuals are all "verify at activation" items owned by the activation runbook.
  3. ESB follow-ups deferred from S3 (unchanged): (a) ambiguous-timeout reconciliation needs a v2 lookup-by-ref contract; (b) explicit release-on-partial; (c) PII_MASK_SECRET → real secret store (activation); (d) short INFLIGHT TTL distinct from the done-TTL (smallest). NEW from S7 chaos: the done-key loss window during a Redis outage is now measured (E-024 nuance) — worth a line in the eventual v2 contract work.
  4. Interview-pack rehearsal (stands): Q3 pitch, Q18 allow-through + ERP dup-ref bridge (NOW ALSO drillable from E-024's measured outage run), Q17 release-failure half-coverage; NEW S7 props: docs/slo-report.md, docs/cost-model.md, tests/chaos/README.md, the H1/H3 refuted-then-fixed narrative, and the cost-sheet's KMS/Kafka dominance story.
  5. If the pack's claims are ever re-cited, cite the shas the gate pinned: CI runs 34643255075@0e5609f, 34645192560@e294948 (historical); Phase-5 gate ran at fc5fbaf..26d3d9e with the green CI run at the post-fix HEAD recorded in E-018. Phase-6 gate: cycle 1 at fd702ce.
open_risks:
  - Budget alarm LIVE path is UNVERIFIED (no account): scripts/budget-alarm.sh dry-run is proven; it targets the doc-verified BssOpenApi `CreateBudget` (2023-09-30, help.aliyun.com/en/user-center/developer-reference/api-bssopenapi-2023-09-30-createbudget) — CLI flag conventions for array args get confirmed at activation (flagged in-script and in E-014).
  - Provider 1.285.0 has NO budget resource (schema-proven). CORRECTED per critic cycle 2: the modern `alicloud_sls_alert` DOES exist in 1.285.0 — migrating off the deprecated `log_alert.notification_list` is a small deferred follow-up (infra/observability/README.md).
  - random_password results land in terraform STATE by design of the current schema usage; at activation, move to KMS-managed secrets + restricted state access (noted in infra/README.md activation checklist).
  - alicloud_alikafka CI policy uses a deliberate `*instances/*` ARN hedge — tighten to the exact ARN form once a live account reveals it (SEC-4-06, activation checklist).
  - Docker daemon is rootless (user-level) and does NOT auto-restart containers after a daemon/host restart — run `make up`; a lost port forward needs full `down`+`up`, not `restart` (and after THIS session's host restart, `up` alone left dead host forwards on pre-existing containers — `docker compose up -d --force-recreate <svc>` per service was the fix; smoke caught them one at a time). `make smoke` (6 services incl. esb-toxiproxy 18474) must exit 0 at every session end.
  - Shared host stacks (busforge at ~/ESB, helios at ~/ETL+): ports 8080/8000/9040/9050/29092 etc. are theirs; NEVER touch/restart those stacks; `ss -ltn` before any new bind.
  - No AliCloud account → Phase 4 ran validated-plans mode per ADR-0002 (Phase 5 inherits it: every cloud-runtime compliance claim is "verify at activation"). CN partition additionally needs a CN-registered account (real-name verification) + ICP for public endpoints (ADR-0005) — designed, plan-validated, never applied; the ICP runbook (docs/icp-filing-runbook.md) exists for the day a public presence lands.
  - Subagent spawn reliability: S4 hit a usage-limit failure on the first SEC-1 spawn (reset mid-session) — git status showed zero debris; a retry succeeded. S3 lesson stands: after any spawn failure, inspect `git status` first. (S6: one critic-spawn interruption left zero debris; retry clean.)
  - k6 is NOT in CI (local evidence only, binary at ~/tools/k6/k6); terraform IS at ~/tools/terraform/terraform (v1.16.2) and in CI via hashicorp/setup-terraform.
evidence_rows_added: [E-001, E-002, E-003, E-004, E-005, E-006, E-007, E-008, E-009, E-010, E-011, E-012, E-013, E-014, E-015, E-016, E-017, E-018, E-019, E-020, E-021, E-022, E-023, E-024, E-025]

## Session Log

### S7 — 2026-09-13
- slice: Phase 6 — observability, load, chaos, cost (the ops-evidence layer). Phases 3/7 not started.
- done: SLO definitions FIRST (docs/slo-report.md committed with honest pending measured columns before any run — definitions-before-measurement is auditable in git history at 1e20128). BUILD-QA subagent built the stepped stress profile + chaos harness + runbook (hypotheses authored before execution, committed at 0e6be2e; analyzer negative-control proven). ORCH-LEAD executed everything: sustained k6 re-proof (E-020), stress ladder to the breaking point incl. the warm-JVM confirmation (E-021), all three chaos recipes (E-022/23/24), then filled the SLO measured columns and the runbook OBSERVED sections FROM the artifacts. SLS alert layer: deprecated alicloud_log_alert migrated to alicloud_sls_alert (schema-exact vs 1.285.0 — the plan validator caught the group_configuration vocabulary live: no_group, not the legacy no_condition), + p95-budget-breach, denied-action-burst, and trail-tamper-tripwire alerts, 4 log_store_index resources, the designed pipeline-metrics store + freshness panel (titled "awaiting Phase 3 producer"); plans re-proven 87/118 (delta +8 accounted); every doc citing 79/110 swept (E-019). Cost model from 10 fetched pricing pages (URL + fetched-on date per row; E-025) drafted by DOC-1 and verified line-by-line. SEC-1 ops-honesty sweep: FIX-FIRST 2 HIGH + 3 MED + 8 LOW, all fixed same session (compliance/phase6-ops-honesty-review.md). Fresh CRITIC gate cycle 1: FAIL 8.50 — caught that the SEC-1 fix commit had marked two application.yml comment edits FIXED when an unasserted string replacement had silently no-op'd, plus spine staleness; all fixed with verified-in-file assertions; cycle 2 spawned.
- measured: E-019 (alert layer, Plan 87/118 + residency/lint re-captured post-review); E-020 (sustained p95 131.6ms / p99 236.2ms @ 19.49 RPS, budget 44%/79% consumed, 352=352 422 accounting); E-021 (breaking point: holds through 90 RPS, warm-110 p95 266ms/p99 349ms at 78.9 sagas/s, cold-110 p95 658ms breach, stock-pool ceiling ~7.9k sagas named, zero infra failures across ~43.9k requests); E-022 (H1 first REFUTED — 8-worker task executor queue-then-30s-timeout, 485 async timeouts, zero breaker/retry activity — root-caused by thread dump, fixed spring.task.execution.pool 32/64/200, re-proved: probes med 3.6s bounded, zero violations); E-023 (kill-erp held first try: 1.9ms fail-fast signature, retry ladder to exhaustion 177 lines, DLQ 0→59 with complete envelopes, first-201 2s after health); E-024 (H3 first REFUTED — Lettuce 60s default timeout made allow-through unreachable: first warn 61s into a 40s outage; fixed 500ms client timeouts; re-proved: allow-through at 0.9s, outage-window 18/18 201 med 1.03s, ERP ORD-DUP-REF backstop visibly holding 422s, done-key loss window honestly recorded); E-025 (cost model: designed 24/7 SG ≈ 949.64 USD/mo, KMS instance 53% + Kafka 32%, 7 URLs re-fetched independently). Standing gates green post-changes: esb-int 8/8, unit 33/33, smoke 6/6, residency 7/7+5/5, plan-sg 87, audit-demo green.
- war stories: (32) **The measured column wanted to be born full.** Writing docs/slo-report.md, my draft came out with specific "Measured (S7)" numbers — before any run existed. Self-caught in the same breath (nothing committed), reset to PENDING, and the definitions-first commit makes the pre/post state auditable. The fabrication reflex is structural: a report template with a measured column INVITES filling it. (33) **An unasserted replace made the honesty layer lie.** Fixing SEC-1's findings, two application.yml comment corrections went through a python str.replace that silently no-op'd (no assert), while the sign-off already said FIXED. The critic caught it (its HIGH). Lesson: a fix-status claim is a claim ABOUT the tree — mechanical edits need assertions or tools that fail loudly (this session's later edits all assert). (34) **The rootless daemon bounced mid-session** (~05:14 UTC: all six sim containers Exited(255) in the same microsecond while helios auto-recovered via its restart policies — sim has none, by design). Recovery was the war-story-30 playbook verbatim (force-recreate per service, smoke). (35) **Both designed degradation modes were invisible until chaos ran under load:** the idempotency layer's allow-through was exception-correct but unreachable behind Lettuce's 60s default (the catch fired 61s into a 40s outage), and the façade's queue-then-30s-timeout sat behind Spring's default 8-worker executor while the breaker and retry ladder — the mechanisms every unit test and the fault-injection suite prove — never saw a failure to act on. Unit-green + suite-green said nothing about bounded degradation; only latency-under-load did. Chaos with a probe instrument is not a nice-to-have layer, it is the only test that exercises time.
- handoff: Phase 6 REVIEW → gate cycle 2. On PASS: Phase 6 DONE with the honest note (SLS runtime + real cloud costs require an account; designed + plan-validated + sim-measured is the claim). The pipeline-metrics producer contract ({"metric":"cdc_freshness_seconds","value":N}) is Phase 3's documented receiving end. ESB follow-up (d) + the v2 lookup-by-ref work should absorb the measured done-key loss window (E-024 nuance). Pack props now include the SLO report, cost sheet, runbook, and the refuted-then-fixed narrative.
- stack state at session end: sim stack healthy (6 containers) + smoke exit 0; ERP/ESB stopped via esb-stop (PID files); ports 18080/18081 released; toxiproxy erp proxy present, no toxics; audit DB left seeded; busforge/helios untouched (daemon bounce mid-session recovered per playbook).
- critic: Phase 6 cycle 1 **FAIL 8.50** (2026-09-13, fresh agent — see ROADMAP gate log): zero crit; 1 HIGH (the unasserted-fix sign-off error, war story 33) + 2 MED (residual request-count claims; spine staleness) + 4 LOW — all fixed with assertions; cycle 2 spawned.

### S6 — 2026-09-13
- slice: Phase 5 completion — compliance & security (the Asia-rollout credibility layer). The S5 stretch had already landed the residency CI suite (E-016); this session delivered the rest of the phase.
- done: DOC-1 subagent drafted the five artifacts from a self-contained prompt (citations constrained to the verified set + new ones web-verified with URLs; three-way honesty split mandated): **compliance/control-matrix.md** (9 legal-obligation rows CM-01..09 + 8 engineering-control rows EC-01..08, every status Enforced-in-build / Designed-schema-validated / Verify-at-activation with file paths + E-rows), **compliance/cross-border-transfer-memo.md** (edge A1 Canada↔Singapore under PIPEDA cl. 4.1.3 + PDPA s.26/reg 10; edge A2 the deliberate CN non-transfer — no egress path, no Art. 38 trigger; Art. 38 mechanism map incl. CAC Measures Art. 4 applicability conditions + Art. 7 filing/PIA; the pseudonymization-is-not-anonymization nuance stated), **docs/icp-filing-runbook.md** (scope: mainland-hosted = filing, SG internal APIs = none; prerequisites; provider-service → pre-review → communications-administration review → number-issued launch gate; footer display; no day counts), **compliance/stride-threat-model.md** (23-row STRIDE over the as-built platform, trust boundaries, findings F-01..F-07 all Applied-with-citation or honestly Deferred), **compliance/audit-trail-design.md** (audit-event schema DDL, ActionTrail field mapping incl. the sourceIpAddress-not-sourceIPAddress catch, 5-query cookbook MySQL+SLS, sim-demo spec). ORCH-LEAD built and ran **scripts/audit-demo.sh + `make audit-demo`**: seeds the schema-conformant `audit_event` table in sim MySQL (8 simulated events, real API vocabulary incl. doc-verified `UpdateRotationPolicy`, RFC 5737 IPs), runs the cookbook, asserts the reads that matter (2 denials visible, StopLogging denial findable, 1 region anomaly, rotation attributable) → **E-017** (positive exit 0 incl. clean bootstrap from DROP DATABASE; mysql stopped → exit 2; idempotent re-run → exit 0; smoke 6/6 after). SEC-1 subagent (read-only, ran everything itself incl. a tamper test) returned **FIX-FIRST (1 HIGH + 4 MED + 6 LOW)** — all 11 fixed same session (HIGH: the runbook's "provider's documentation states no fixed duration" was one-fetch-falsifiable — the cited page publishes 1–2/1–20 business-day windows; reworded to acknowledge the published window without quoting numbers, in runbook + cn-partition README; MEDs: ICP quote-to-URL misattribution fixed with the requirements page's real sentences, E-017 ledgered + tracked + cited, STRIDE run-ID home fixed, F-01 provenance remapped; LOWs: ESB→ERP cleartext-PasswordText threat row + F-07, ESB→Kafka SASL row, Art. 38 catch-all/treaty nuance + Art. 4/7 checklist enrichment, separator wording, license-context hedge, softened provider-enforcement claim, two code-line cites) → recorded in compliance/phase5-security-review.md with the fixes addendum. Fresh CRITIC gate (cycle 1) → **PASS 8.8**, 10/10 drill questions SURVIVES, zero crit/high (its 1 MED + 2 LOW — spine staleness, CI-run-at-HEAD, pack Q28 phrasing — all fixed same session). Pack updated additively (Q29 anchor + whiteboard 18:30–21:00 now cite the matrix/memo/E-017; Q28 launch-gate wording). ROADMAP row 5 closed.
- measured: E-017 (audit demo: positive/negative/restore/idempotent + smoke-after all captured; SEC-1's independent tamper probe turned it red — assertions bite); E-018 (CI 4/4 green at the post-fix HEAD, incl. the residency step); residency 7/7 + selftest 5/5 re-proven repeatedly (ORCH-LEAD, SEC-1, CRITIC); plan-sg `Plan: 79` re-proven; smoke 6/6 re-proven after the session-start host-restart recovery; gate cycle 1 weighted **8.8** (correctness 9, arch 9, AliCloud-authenticity 9, integration 9, audit-rigor 8.5, sec-compliance 9, test-quality 9, docs 8, evidence 8.5, defensibility 9)
- war stories: (28) **A deliberate omission became a false claim about a source.** The no-day-count discipline ("quote no ICP review durations") was implemented as "the provider's documentation states no fixed duration" — which SEC-1 falsified with one fetch: the cited page publishes 1–2 and 1–20 business-day windows. The rule bans quoting a number; it never licensed asserting an absence. The honest form: acknowledge the published typical window exists (non-binding, cannot be expedited), record no figure, gate the launch on the filing number. Same genre as the PDPA misquote (24): claims ABOUT a source die when someone opens the source. (29) **The demo's bootstrap had a deadlock the first capture caught**: passing `silkroute_audit` as mysql's default database makes the client refuse to start when the database doesn't exist — so the DDL that would create it never ran. Fixed by splitting the DDL path (no default DB) from the query path; the artifact now proves clean bootstrap from DROP DATABASE. (30) **After a host restart, `make up` was not enough**: pre-existing containers restarted healthy but with dead rootlesskit host forwards — smoke failed them one at a time (redis, then mysql) until each was `--force-recreate`d. `up` recreates missing containers, not broken forwards; the falsifiable smoke (E-003's design) is what turned an invisible breakage into loud, specific failures. (31) **Two commit subjects broke the project's own commit-style rule** (one named an evidence row, one carried the review score) — self-caught at wrap-up while screenshotting the Actions page. Pushed history is append-only by design, so they stand as-is and are recorded here rather than rewritten; the rule exists so the public log reads human, and the misses are visible in it.
- handoff: Phase 5 DONE (honest note: runtime compliance evidence still requires an account — every artifact carries the verify-at-activation boundary). Remaining depth phases: 3 (ETL), 6 (observability/load/chaos/cost), 7 (docs & demo). Phase 3 would flip pack Q31–Q34 from design-only to measured. The compliance artifacts are interview props as of now — rehearse citing them by name (next_actions #3).
- stack state at session end: sim stack healthy (6 containers) + `make smoke` exit 0; audit DB left seeded in sim MySQL (idempotent on re-run); ERP/ESB ports released; no stray java (no esb-run this session); busforge/helios untouched.
- critic: Phase 5 cycle 1 **PASS 8.8** (2026-09-13, fresh agent — see ROADMAP gate log). Subagent chain: DOC-1 (five drafts) → ORCH-LEAD (audit demo + integration) → SEC-1 (FIX-FIRST → 11 fixes) → CRITIC (gate). All subagent claims re-run by ORCH-LEAD before recording; SEC-1's HIGH was independently confirmed by ORCH-LEAD's own fetch of the provider page before rewording; the critic then verified the fixes and drilled the docs 10/10.

### S1 — 2026-09-08
- slice: Phase 0 — Foundation (scaffolding, state machinery, mode decision, sim compose skeleton)
- done: git repo + full §3 tree committed (dirs carried via .gitkeep); ROADMAP/STATE/EVIDENCE machinery; ADR-0001 (dual-mode sim/cloud); ADR-0002 (sim-only now, validated-plans for Phase 4); docs/scenario-charter.md; docker-compose.yml sim network (mysql/kafka/redis/minio/toxiproxy, healthchecked, 127.0.0.1-only host binds, .env-indirected sim credentials, kafka dual-listener w/ host port 39092); scripts/smoke.sh (falsifiable 5-service assertions, container-env credentials, negative control proven); Makefile (up/down/ps/logs/smoke with jq guard); README (prereqs + host-port table); CI scaffold (unverified, no remote yet); .gitignore; commits
- measured: clean start converged ~28s, 4 (healthy) + toxiproxy Up (distroless, API on :8474), kafka HOST listener accepting 127.0.0.1:39092 → E-001; repro `down -v → up` + `smoke OK (all 5 services) in 2s` → E-002; negative control: redis stopped → smoke exit 2, restored → exit 0 → E-003
- war stories: (1) system Docker daemon can't be started (no passwordless sudo); rootless dockerd was already alive — first `dockerd-rootless.sh` attempt hit a RootlessKit lock collision proving another instance held the state dir; fixed with persistent `docker context use rootless`. (2) `shopify/toxiproxy:2.11.0` doesn't exist on Docker Hub (tags stop at 2.1.4) — GHCR carries new releases; and GHCR images are distroless, so a `wget` healthcheck can't work — readiness must be asserted host-side. (3) Port 6379 collides with a pre-existing local Redis → sim remaps host port to 16379. (4) `make smoke` v2 could not fail: in `set -e` shells, a failing command that is a non-final member of an `&&` list is exempt from errexit — my `check && echo OK` lines never propagated failure; the critic stopped redis and smoke still said OK. Rewrote as scripts/smoke.sh with explicit `|| fail` on every assertion + committed negative-control evidence (E-003). (5) This is a SHARED host: an unrelated Airflow stack runs on the same rootless daemon, and a failed kafka bind leaked port 29092 inside the rootlesskit port driver — unrecoverable without restarting the daemon (would kill the other stack), so Kafka's host port moved to 39092. Lesson: on shared/rootless hosts, port choice is a scarce resource and daemon restarts are off the table.
- handoff: WSDLs not yet authored — Phase 1 session starts with contract-first authoring, NOT Java code; record JDK 17 setup in the S2 log
- critic: cycle 1 FAIL 7.80 (evidence row not bound to command/artifact; tree not in git; STATE over-claims; credential/port hygiene; jq undeclared; CI unverified) — fixed. Cycle 2 FAIL 7.75 (crit: smoke could not fail — set -e &&-list exemption, proven live by critic; hardcoded creds in smoke/healthcheck; kafka host listener unusable; README port omissions) — all fixed same session: scripts/smoke.sh with `|| fail` assertions + container-env credentials + negative-control evidence E-003 + kafka dual-listener on 39092. Cycle 3: critic spawn failed twice (quota/model errors) — gate OPEN, self-audit recorded, fresh critic required at S2 start. See ROADMAP gate log.

### S1-resume — 2026-09-09
- slice: cycle-3 critic attempt + ORCH-LEAD self-audit (no new build work)
- done: 2 critic spawn attempts failed (exceed quota limit / model request failed); pivoted to honest self-audit — secrets scan (only ${VAR:-default} remain, matching .env.example), compose config OK, git clean, §3 tree confirmed in git, negative path re-proven
- measured: recovered sim stack after a daemon/host restart (all containers Exited(255)) — `make up` restored 4 healthy + toxiproxy in ~21s with volumes intact; smoke then CAUGHT a dead toxiproxy host-forward (8474 refused) — falsifiable smoke proving its worth; `docker compose restart toxiproxy` did NOT re-establish the rootlesskit forward, only a full `down` + `up` did (8474 released and re-bound); re-ran positive/negative/restore smoke: exit 0 / 2 / 0 → E-003 artifact refreshed with full transcript
- war stories: (6) rootless Docker + host restart: containers don't come back on their own (no restart policies — deliberate for a sim stack), and a lost port forward is NOT recoverable via `docker compose restart <service>` — the binding must be fully torn down (`down`, without -v to keep volumes) and re-created. Without a smoke that can fail, the dead 8474 forward would have looked "green" all day.
- handoff: gate OPEN → S2 must start with fresh CRITIC cycle 3 (see next_actions), then Phase 1

### S2 — 2026-09-09
- slice: Phase 0 gate closure (cycle 3) + Phase 1 complete (legacy ERP, WSDL-first, frozen)
- done: **Phase 0 DONE** — fresh CRITIC cycle 3 PASS 8.50 (critic ran the negative path live itself); its 4 must-fixes applied before Phase 1 build work: host-forward TCP probes for all 5 published ports in scripts/smoke.sh (the rootlesskit failure mode now machine-checked), E-003 re-captured with stderr, E-001 reproduce command bound to the kafka host probe, mysql exec failure path routed through `fail`. **Phase 1 done**: Maven wrapper bootstrapped (pinned 3.9.9, `bin` type — host mvn 3.9.9 was used once to bootstrap; repo no longer needs it); contracts authored FIRST by ORCH-LEAD (3 WSDLs SOAP 1.2 doc/literal + 4 XSDs, typed faults on ErpFaultType, MoneyType integer minor units + CAD/SGD/CNY enum, StoreRegionCodeType CA/SG/CN), xmllint-validated incl. instance positive + negative (USD rejected by enum) and committed (bcc113a) before ANY Java; BUILD-LEGACY subagent built Spring Boot 3.3.5 + CXF 4.0.5 via cxf-codegen wsdl2java from the frozen WSDLs, WSS UsernameToken, in-memory ledger 50 SKUs / 8 stores / 400 rows, demo generator; ORCH-LEAD re-verified 16/16 over the wire; BUILD-QA subagent built the Karate 1.5.2 contract suite (16 scenarios, black-box boot w/ hard-fail falsifiability, namespace-aware local-name parsing) + CI job; ORCH-LEAD re-ran everything (E-005/E-006); **WSDLs FROZEN per C6: ADR-0003 + tag `contract-freeze-erp-v1` (commit bba1982)**; Phase 1 critic PASS 8.85 (see ROADMAP gate log) — 4 of 6 follow-ups closed same session (Receiver semantics → ADR-0003; E-006 probe → scripts/erp-verify.sh + row rebound, re-proven 16/16; CI drop -DskipTests; STATE root-pom strike), 2 deferred to S3 start
- measured: contracts 7/7 xmllint OK, instance validation PASS, negative (USD) rejected; unit 15/15; Karate contract 16/16 (pricing 4, orders 6, inventory 4, security 2) — booted jar on 127.0.0.1:18090, dead-port red path proven; runtime `ERP SEED: skus=50 stores=8 stockRows=400` + `ERP DEMO: requested=5 submitted=5 skipped=0`; ORCH-LEAD probe PASS=16 FAIL=0 twice (incl. committed scripts/erp-verify.sh); critic independent PASS 8.85 → E-004, E-005, E-006
- war stories: (7) **The critic-proof WSS debugging saga**: my independent verification failed 13/16 with generic WSS4J "security error" while BUILD-LEGACY's claims said green. Two lessons. First, the wire is stricter than blog folklore: Nonce requires EncodingType (BSP:R4220) and the Password `Type` MUST be the OASIS **UsernameToken profile** URI (`...username-token-profile-1.0#PasswordText`) — I had the secext *element* namespace (`...wssecurity-secext-1.0.xsd#PasswordText`), which WSS4J classifies as an unknown custom password type and rejects (UsernameTokenValidator line 96, handleCustomPasswordTypes=false). Second, I nearly "fixed" the wrong thing: removed the service's `passwordType` property after misreading WSHandler.decodePasswordType — PW_TEXT ("PasswordText", the short selector you configure) and PASSWORD_TEXT (the profile URI WSS4J requires on the wire) are DIFFERENT constants, and I only proved the service was right by mapping exact validator line numbers (79 = required-type check, 96 = custom-type rejection) against the shipped 3.0.3 sources. REVERTED my edit; fixed my tool instead. Discipline: change one variable at a time; read the shipped library source, not memory. (8) `pkill -f legacy-erp` killed my OWN compound shell twice — the pattern matches any cmdline containing it, including the very command doing the killing (and `pgrep -f '[l]egacy-erp'` doesn't save you when the same command line elsewhere references the path). Kill by recorded PID file. (9) Subagent spawn failure is not clean: the first BUILD-QA spawn died on `user concurrency limit exceeded` but left a half-built RED suite behind; the retry inherited it, diagnosed, and fixed — after any spawn failure, inspect `git status` before assuming a clean slate. (10) Karate 1.5.2 auto-parses responses keyed by literal namespace PREFIXES, and CXF prefixes are throwaway (`soap:`, `ns2:`) — contract tests must parse on local names (BUILD-QA's DOM parser does, with doctype/XXE hardening).
- handoff: Phase 1 frozen — Phase 2 (ESB core) starts with the 2 deferred contract-suite scenarios (next_actions #1), then canonical model + Camel. ERP runs on 18080, tests on 18090; sim creds esb-client / erp-wss-pass-2026 (sim dummies). CXF serves the frozen contract re-serialized (comments stripped, imports rewritten) — contract tests are the integrity check, NOT byte-diffing the served WSDL.
- stack state at session end: **LEFT HEALTHY AND RUNNING** (choice): sim-mysql/kafka/redis/minio (healthy) + sim-toxiproxy, `make smoke` exit 0 with host-forward probes; no ERP processes left; ports 18080/18090 released; unrelated busforge/helios stacks untouched.
- critic: Phase 0 cycle 3 **PASS 8.50** (fresh agent; ran stop-redis negative path itself). Phase 1 cycle 1 **PASS 8.85** (fresh agent; re-ran builds + own wire probes + dead-port falsifiability + freeze-tag diff). Both verdicts in ROADMAP gate log. Standing obligation: first green GitHub Actions run becomes an evidence row once a remote exists.

### S3 — 2026-09-11
- slice: Phase-1 deferred follow-ups (Part A) + Phase 2 complete build (ESB core) — Part B
- done: **Part A** — 2 security.feature scenarios added (missing-header auth fault on /ws/orders/v1 cross-endpoint; identical-envelope nonce-replay rejected by the live server cache → contract suite 16→18, green); ErpFaults emits xsi:nil for the nillable invalidValue (2 new unit tests marshal-assert `xsi:nil="true"` on the wire); E-005 refreshed honestly (17 unit / 18 contract). **Part B — apps/esb**: canonical Order JSON Schema v1 (urn:maple:canonical:order:v1, enforced by camel-json-validator/networknt); Camel 4.10.3 LTS + Spring Boot 3.4.5 (deviation from the brief's "3.3.x" suggestion: verified Camel SB 4.10.3 is built against Boot 3.4.x — spring-boot-version=3.4.4 in its parent pom — and 3.4.5 is the patch release on Central; recorded here as the pin decision); REST façade on platform-http (loopback 18081); hand-rolled saga orchestrator order→reserve(per line)→pricing→confirm with compensation; CXF 4.0.5 client generated from the FROZEN WSDLs via wsdl2java pointing at ../legacy-erp (single source of truth) + WSS4J UsernameToken OUT interceptor (profile-URI Password Type, fresh nonce) — WSS auth worked FIRST TRY on the wire this time, the S2 wire-facts doc paid off; 4 real XSLT 1.0 stylesheets for the canonical↔legacy XML legs (unit-tested incl. validation against the frozen XSDs); CBR by storeId prefix visible in the response `route` object; retry 3×200ms×2.0 with real attempt counting; resilience4j circuit breaker (50%/10/6/2s/3, infra-only) + `throwExceptionWhenHalfOpenOrOpenState(true)` (load-bearing — default silently passes an OPEN breaker); Kafka DLQ + orders.events topics; Redis idempotent consumer (SETNX+TTL, key = Idempotency-Key header else sha256(sourceSystem+externalOrderRef)); PiiMaskingPolicy C1 hook (CN customerRef → `msk-`+sha256[0..11] on every shared-topic egress); ErpErrorClassifier maps ErpFaultType detail → Sender-class BUSINESS despite the wire's soap:Receiver (ADR-0003 acceptance item). BUILD-QA: 8-scenario black-box fault-injection suite (tests/esb-int, falsifiable boot of both jars, PID-file hygiene), toxiproxy chaos recipes (tests/chaos/esb-faults.sh), k6 profile (tests/load), new CI job `esb-integration-tests` (additive; existing 2 jobs untouched). Root pom aggregator: NOT adopted (builds stay `-f <module>`; recorded decision).
- measured: esb unit 32/32; contract 18/18 (after Part A); **integration suite 8/8** (~80s): retry attempts=3 under persistent 2500ms latency; ambiguous-timeout retry attempts=2 with ERP ORD-DUP-REF guard holding; hard-down → CIRCUIT-OPEN fail-fast **4ms** + DLQ + half-open recovery 201; compensation proven by ERP stock 19→19 with releasedReservationId; CN DLQ masking `msk-*` (clear value absent); **k6 p95 = 44.25ms** @ 18.63 RPS (1304 iters, medians 18.25ms, 0 infra failures; the 18 non-201s are 422 INV-OUT-OF-STOCK from seeded 5–100-unit stock rows legitimately exhausted) → E-008, E-009, E-010
- war stories: (11) **The double-encoding bugs**: both agents' unit tests were green yet every integration JSON read returned empty — the REST response body and the success events were JSON-in-JSON. Root cause: `writeValueAsString()` applied to an ALREADY-serialized JSON string, in TWO places (saga response at the HTTP boundary; success-event publish). The fault-injection suite caught what 32 unit tests could not, because the bug lived exactly at the serialization boundary. Lesson: wire-format assertions must run over the real endpoint; shape-of-the-payload is a black-box concern. (12) **Rootless-Docker "localhost" is asymmetric**: the brief said route ERP calls through toxiproxy on 8474 — but bridge-mode sim-toxiproxy cannot upstream to a host-loopback service (the container's 127.0.0.1 is itself; tested the slirp alias 10.0.2.2 live — dead in this rootlesskit setup). Fixed with a dedicated host-network toxiproxy (sim-esb-toxiproxy, API 18474, proxy "erp" on real host loopback 18180→18080) committed into the compose stack; also learned docker compose does NOT recreate a service on port-map changes unless `up -d` is re-run for it, and a stale host port-forward lingers until then. (13) **Timeout ≠ failure, demonstrated live**: under the transient-latency recipe, the ESB's submitOrder attempt 1 timed out while the delayed ERP actually committed the order; attempt 2 then hit the frozen ERP's duplicate-ref guard. The suite asserts BOTH honest endings (201 clean recovery / 422 ORD-DUP-REF with attempts∈[2,3]) — retry provably fired AND no duplicate order can exist. Proper fix (reconciliation on ambiguous timeout) needs a v2 lookup-by-ref the frozen estate doesn't offer — recorded as S4 follow-up. (14) **Parallel agents drift**: BUILD-ESB and BUILD-QA ran concurrently against one pinned contract and still drifted (error field `code` vs `errorCode`; envelope flat vs nested). The orchestrator's own integration run — not either agent's green report — is the real gate.
- handoff: Phase 2 gate in flight (critic next). Runbooks: apps/esb/README.md (run + fault-injection hook + config), tests/chaos/esb-faults.sh (human toxiproxy entrypoint). `make` targets: esb-int / esb-run / esb-stop / load-orders. k6 binary at ~/tools/k6/k6. ERP_WSS dummies moved to env-indirection only in ESB (YAML); ERP module untouched (C6). S4 starts with the critic verdict + next_actions #2/#3.
- stack state at session end: sim stack healthy + `make smoke` exit 0 (now 6 containers incl. sim-esb-toxiproxy); toxiproxy "erp" proxy present, no toxics; ERP/ESB ports released; no stray java processes (PID-file kills only); busforge/helios untouched.
- critic: Phase 2 cycle 1 **FAIL 8.10** (2026-09-11, fresh agent — see ROADMAP gate log): the critic independently re-ran everything AND reproduced a real defect the suite missed — a FAILED saga never released the Redis idempotency claim, so a client retrying the same key got 409-locked out for the TTL. All blockers/meds/lows fixed the same session (commit 09dcf7c): idempotency release + regression scenario (s6b), breaker-free compensation route, keyed-HMAC pseudonymization with honest javadoc, single-run pid-bound E-009 artifact, live-captured 409 in E-008, 5 doc/comment fixes. Re-verified: unit 33/33, suite 8/8, CI 3/3 jobs green incl. first esb-job run → E-011. Cycle 2 spawned after this entry.
- war stories (post-cycle-2 CI fix): (23) **The sim stack broke WITHOUT any repo change** — the third CI failure was not rate limiting (instant, repeatable denials): minio PURGED its old release tags from Docker Hub mid-session (identical compose green at 19:14, `pull access denied` from 19:43). Durable fix: same tag, quay.io (`quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z`) — registry pinning is part of version pinning, and ADR-0001's parity-drift concern applies to the registry as well as the tag. Local stack unaffected (image already cached) — proof that a sim that "runs fine on my machine" can be CI-dead an hour later.
- critic: Phase 4 cycle 2 **FAIL 8.36** (2026-09-11, fresh agent — ROADMAP gate log): zero crit/high; all cycle-1 blockers verified genuinely fixed. 5 med + 4 low, all addressed same session: CI retry wrapper + flake record, alicloud_sls_alert schema-claim corrections (ADR-0004 + STATE), SetBudgets risk line retired, 75/106 → 79/110 everywhere, snat depends_on, lint hardening, observability contact/dashboard honesty notes. Re-verified: plans 79/110 unchanged, fmt clean, lint OK + var-ref negative control red. Cycle 3 spawned after this entry.
- critic: Phase 4 cycle 3 **PASS 8.57** (2026-09-11, fresh agent — ROADMAP gate log): zero crit/high; every cycle-2 blocker reproduced as fixed (green CI at HEAD run 34643255075, snat depends_on, sls_alert claim, stale counts, lint falsifiable in both negative directions). Remaining findings were evidence-hygiene (uncommitted green-run record, stale SetBudgets mentions in devlog/STATE history) — closed in the gate-closure commit. **Phase 4 DONE.**
- critic: Phase 2 cycle 2 **PASS 8.50** (2026-09-11, fresh agent — ROADMAP gate log): all 9 cycle-1 findings verified fixed in code AND behavior; critic's own live probes reproduced the cycle-1 high and confirmed the fix on an uncovered path. Zero blockers. Cycle-2 med (smoke/README coverage for esb-toxiproxy) closed in the wrap-up — smoke now asserts 6 services incl. 18474 and was re-proven exit 0; 3 lows fixed or recorded as S4 follow-ups. **Phase 2 DONE.**
- stack state at session end (final): sim stack healthy + smoke exit 0 (6 services); ERP/ESB ports released; no stray java; busforge/helios untouched.

### S4 — 2026-09-11
- slice: Phase 4-lite — AliCloud landing zone in validated-plans mode (ADR-0002): infra/ terraform, SEC-1 IAM review + fixes, budget alarm, CI wiring
- done: terraform 1.16.2 installed at ~/tools/terraform (no system terraform; provider-download mechanics de-risked BEFORE spawning: plan with placeholder creds performs no API calls — proven on a /tmp spike first). BUILD-CLOUD subagent authored infra/: network (VPC/vSwitches/NAT/SGs, no 0.0.0.0/0 inbound), security (KMS keys+aliases, esb-runtime RAM role+policy, CI user→assume-role chain, explicit-action deploy policy), data (RDS MySQL 8, OSS lake+artifacts SSE-KMS/versioned/public-access-blocked/SecureTransport-denied), compute (SAE apps silkroute-esb/erp + alikafka instance/topics byte-identical to sim topic names), observability (SLS project/stores/dashboard/alerts, CMS alarms, ActionTrail→SLS), cn-partition (mirror partition, residency tags, no egress path) behind `var.enable_cn_region`, and root compose + backend-remote.example (oss backend + tablestore lock) + docs/infra-env-parity.md (sim env names ↔ managed services, ADR-0001 no-code-change rule). ORCH-LEAD verified everything itself (E-012). SEC-1 subagent reviewed IAM/network/residency/secrets → FIX-FIRST (1 high, 3 med, 6 low); ALL must-fixes + actionable lows fixed same session: **the high — CN partition was NOT provider-region-pinned** (tags/name-strings only; would have landed in ap-southeast-1) → aliased `alicloud.cn` provider + `providers` meta-argument; runtime OSS over-grant split per the data module's matrix (artifacts r/o, bronze rw); `kms:GenerateDataKey` added (SSE-KMS uploads need it); silkroute-ci got its minimal sts:AssumeRole policy (RAM requires caller-side grant); TDE+tde_encryption_key on BOTH RDS instances; VPC-only security_ips; CN topic residency tags; SLS ARN region pins; trust-boundary + activation-checklist README sections. Budget alarm: provider 1.285.0 has NO budget/bss/billing resource (schema-proven) → scripts/budget-alarm.sh (BSSOpenAPI SetBudgets [corrected in cycle 1 → CreateBudget, BssOpenApi 2023-09-30], dry-run default, live path honestly flagged verify-at-activation) + guarded make deploy-sg/destroy (double guard, refusal proven exit 2) + make plan-sg (the one-command AC). CI: 4th job terraform-landing-zone (fmt/init/validate/plan SG + plan CN). ADR-0004 (SAE over ACK-spot/FC + budget-alarm mechanism), ADR-0005 (region strategy: SG hub; CN designed+plan-validated, never applied).
- measured: fmt exit 0; validate green 6/6 modules + root (1 documented deprecation warning); **Plan: 75 to add (SG), 106 to add (CN flag on)** post-fix (73/104 pre-fix; +2 = the new ci_assume policy+attachment) → E-012; SEC-1 verdict + fixes → E-013; budget dry-run + guard refusals + plan-sg positive control → E-014; wildcard-action grep (15-line window) = 0
- war stories: (15) **Region pinning lives at the provider level, not in names or tags** — every "cn-beijing" string in the CN module was real and the placement was still Singapore, because there was one unaliased provider. Tags are theater until the resource graph enforces the constraint; SEC-1 caught what would have been the C1 story-killer. Terraform lesson: `providers = { alicloud = alicloud.cn }` is where residency actually lives; the 104-resource green plan proved schema, not placement, until this fix. (16) **The fabrication trap is structural, not moral**: the phase plan said "budget alarm in terraform" and the provider simply has no such resource (1161 resources, zero budget/bss/billing). The honest mechanism (executable BSSOpenAPI script + dry-run + schema-gap proof) is BETTER interview material than a fake resource would have been — but only because the schema grep came first. Check the vocabulary of the provider before designing against it. (17) Usage-limit spawn failure mid-session (SEC-1, first attempt): `git status` showed zero debris (the agent died before writing), the retry succeeded clean — the S2/S3 spawn-debris rule cost 30 seconds, not a session.
- handoff: Phase 4 gate next (critic). Terraform is NOT on PATH — use ~/tools/terraform/terraform or make targets. `make plan-sg` is the acceptance command; CN plan needs `-var enable_cn_region=true`. CI now 4 jobs; watch the first terraform job run. Phase 5's residency CI tests can seed from SEC-1 §5 greps. ESB follow-ups (d) short INFLIGHT TTL is the smallest spare-time item.
- stack state at session end: sim stack re-upped mid-session after a host restart (containers Exited(255), daemon fine) — `make up` + `make smoke` exit 0 (6 services); ERP/ESB ports released, no stray java (no esb-run this session); busforge/helios untouched.
- war stories (gate cycle 1 addendum): (18) **Validated-plans mode has a structural blind spot: plan proves schema, not apply.** The critic's crit was exactly that class — ApsaraMQ for Kafka CreateTopic rejects dots in topic names, so the "sim byte-identical topic names" parity claim could never survive an apply, yet every plan was green. Fix is three-layered: dot-free cloud topic names selected via new KAFKA_ORDERS_TOPIC/KAFKA_DLQ_TOPIC env indirection in the ESB (the ADR-0001-sanctioned mechanism — sim defaults untouched, 33/33 unit tests still green), a new scripts/tf-apply-validity.sh lint in CI guarding plan-invisible API rules (negative control proven), and the honest correction of every doc that made the claim. (19) The budget-script blocker was war story 16 missed one layer up: I schema-grepped the provider but wrote the API name from memory — `SetBudgets` isn't in the docs; the real contract is `CreateBudget` (BssOpenApi 2023-09-30), verified against the live doc page before rewriting. Lesson: cite the doc URL in the artifact, not just the claim. (20) Basic AliCloud security groups are DEFAULT-ALLOW on egress — my "explicit egress kept minimal / denied by default" comment was prose, not enforcement (same genre as SEC-4-01, caught by the critic this time). Egress honesty now stated in code comments + review addendum; advanced groups deferred to activation.
- war stories (gate cycle 2 addendum): (21) **Red CI at HEAD is a discipline failure even when the flake isn't your code** — the fix commit's sim job died twice on a Docker Hub anonymous-pull denial (`minio/minio`), and my first instinct was to call it transient; the critic's point is sharper: a CI-proof claim with a red unrecorded HEAD is the project's own cardinal sin. Fix: retry wrapper (3×, 45s) around `make up` in both docker jobs + the flake (2 attempts, cause, resolution) recorded inside the E-012 artifact. (22) **Applying fixes can re-introduce the bug class they close** — my snat_entries were plan-valid but racy at apply (CreateSnatEntry needs the EIP associated first); `depends_on` now encodes the API prerequisite. Plan-invisible hazards keep surfacing: the lint now also rejects variable-RHS topic names.
- critic: Phase 4 cycle 1 **FAIL 7.21** (2026-09-11, fresh agent — see ROADMAP gate log): critic re-verified ALL SEC-1 fixes live, then found 1 crit + 2 high + 5 med + 4 low, all fixed same session (topic names, budget API, egress+SNAT, KMS trim, private_2, stale numbers, evidence commit discipline). Re-verified: fmt clean, validate Success, plans 79/110, esb unit 33/33, lint OK + negative control red, guards exit 2. Cycle 2 spawned after this entry.

### S5 — 2026-09-12
- slice: Phase 8 — resume & interview pack (the fast-path close-out). Phases 3/5/6/7 deliberately NOT started.
- done: DOC-1 subagent drafted the pack (docs/interview/resume-bullets.md, qa-deep-drill-40.md, whiteboard-walkthrough-25min.md, what-id-do-differently.md + demos/video-outline.md) from a self-contained prompt quoting §2/§9 + the evidence table; ORCH-LEAD integration pass verified every number/claim against the ledger and the code (CIDRs, bucket names, KMS aliases, SLS stores/retention, SG rule sources, breaker/retry tuning, Principal-wildcard count), fixed the 5→6-service sim drift with a fresh pack-time re-verification row (**E-015**: smoke 6/6 exit 0 + `make plan-sg` → `Plan: 79 to add`, 2026-09-12); SEC-1 subagent independently re-derived all 14 statutory/regulatory citation instances from primary sources (SSO, Justice Laws, CAC, Alibaba ICP docs, two PIPL translations) → FIX-FIRST with ONE substantive finding: the pack's PDPA s.26 "quotation" existed in neither the 2012 nor current statute (the "appropriate steps" language is PDP Regulations 2021 reg. 10) plus a false version-history note — BOTH fixed against the statute of record with URLs, + optional hardening applied (Art. 38 catch-all/treaty nuance, ICP "public" shorthand, process URL, whiteboard C1 framing); fresh CRITIC (cycle 1) ran the 3-round hostile mock interview → **PASS 9.27**, zero crit/high: R1 re-ran the ACs live itself (smoke, plan-sg 79, CN plan 110, budget dry-run, wildcard grep 0, provider schema 1161/0 budget resources, green CI runs) and mapped every number to its row + re-verified the corrected PDPA quote verbatim; R2 ten hostile follow-ups → 10 SURVIVES / 0 COLLAPSES; R3 cross-file consistency + hygiene clean. Its 2 med + 5 low all fixed the same session (7ce09be): Q37 "earned its keep twice" → once (the log records one catch); differently-#2 misattributed the idempotency-lockout catch to the suite instead of the review; stale E-003 FAIL-line quotes refreshed to the artifact's actual line; CI shas pinned in the resume bullets; E-001…E-014 → E-015 range; CI step labels 5→6 services / 16→18 scenarios; Q33 now pre-empts the ApsaraDB-Redis-not-modeled gap. ROADMAP marks **v1 resume-ready** with phases 3/5/6/7 explicitly TODO.
- measured: E-015 (smoke 6/6 exit 0, plan-sg `Plan: 79 to add, 0 to change, 0 to destroy.`, exit 0); citations 14/14 instances re-derived, 1 substantive misquote caught and fixed; gate cycle 1 weighted **9.27** (no re-cycle needed — zero crit/high); pack = 11 resume bullets + traceability table, 40 Q&A (10 AliCloud / 10 ESB-SOAP / 4 ETL-design-only / 6 compliance / 6 delivery / 4 war stories), 25:00 whiteboard + 10:00 collapse, 8 do-differently answers, 10-section video outline
- war stories: (24) **The citation check caught a misquoted statute — the SetBudgets lesson one layer up.** DOC-1 DID verify its citations (fetched a PIPL translation and regulator guidance) but for PDPA s.26 it quoted wording that circulates in secondary commentary — "shall not transfer … unless the organisation has taken appropriate steps …" — which appears in neither the 2012 original nor the current statute; the "appropriate steps" language lives in reg. 10 of the PDP Regulations 2021, and the "original text read 'is satisfied'" claim was also false. The mechanical rule ("cite the doc URL in the artifact") was necessary but not sufficient — the URL pointed at the right STATUTE while the quote came from the wrong LAYER (commentary vs text). What made the difference was a second, independent verifier re-deriving quotes from the primary source instead of trusting the first pass's log. Two verifiers beat one verifier with better intentions. (25) **The pack outran its own log in two small ways** — "earned its keep twice" where the log records one catch, and crediting the wire-level suite for a catch the independent review actually made. Both were caught by a fresh reader cross-checking STATE.md against the pack; embellishments survive solo writing and die under fresh cross-reading, which is exactly why the gate is a fresh agent every time.
- handoff: v1 resume-ready — S6 starts with next_actions #1 (Phase 5 residency CI tests are the natural continuation; their seed: compliance/iam-review-phase4.md §5 greps + E-013's tag adjudication + the Q29 gap statement). The Phase 5 stretch was NOT started at wrap-up — it was taken in the S5-addendum below (session resumed). Pack rehearsal notes in next_actions #3.
- stack state at session end: sim stack healthy (6 containers) + `make smoke` exit 0 re-proven (E-015); ERP/ESB ports released; no stray java (no esb-run this session); busforge/helios untouched.
- critic: Phase 8 cycle 1 **PASS 9.27** (2026-09-12, fresh agent, 3-round hostile mock interview — see ROADMAP gate log). Subagent chain: DOC-1 (drafts) → SEC-1 (citations, FIX-FIRST → fixed) → CRITIC (gate). All subagent claims re-verified by ORCH-LEAD before recording; the citation catch and both embellishments were found by the independent checks, not by the drafter.

### S5-addendum — 2026-09-12 (same session, post-wrap-up)
- slice: the authorized stretch — Phase 5's residency CI tests (seed: SEC-1 review §5 item 4 + E-013 tag adjudication). Phase 5 is now IN_PROGRESS; everything else in the phase (control matrix, transfer memo, ICP runbook, STRIDE, audit demo, SEC-1 sign-off, gate) remains.
- done: `scripts/residency-tests.sh` — 7 falsifiable static checks (R1 provider-graph CN pinning + count flag; R2 CN-string confinement; R3 zero replication/backup/mirror resources; R4 residency tags on every taggable CN resource with the schema-tagless adjudication list; R5 bucket-family separation with the management-plane ARN prefixes allowed; R6 no NAT/EIP in CN; R7 single Kafka publish chokepoint through PiiMaskingPolicy + saga maskForEgress wiring) + a `--selftest` negative control that injects 5 violations and requires each check to catch its own. `make residency` target; CI step added INSIDE the existing terraform job (still 4 jobs — the pack's "4-job pipeline" bullet stays true).
- measured: green run 7/7 exit 0; selftest 5/5 mutations caught → **E-016** (evidence/runs/E-016-residency-tests.txt).
- war stories: (26) **The first version of the suite could not fail — and only the selftest knew.** `failed_ids=$(run_checks)` ran all checks inside a command-substitution subshell: the failure counter never reached the parent (suite always exited 0) AND every FAIL line was swallowed into the variable (the selftest, which greps the output for `FAIL [id]`, saw only the final "OK" line). Green 7/7 was printed while three checks (R1/R4/R5) were genuinely failing on the real tree — R1's grep used `\?` (BRE "optional") where the file has a literal `?`; R4 streamed multi-line HCL bodies through a line-reader so every continuation line became a phantom untagged block; R5 tripped on the CI deploy role's legitimate `silkroute-cn-*` ARN family prefixes (management plane, adjudicated in SEC-1 §2). The negative control converted an invisible three-way failure into three loud ones — the same lesson as war story 4, now institutionalized: the selftest runs in CI next to the checks it polices. Also (27) small: the selftest's own mutation initially used `tags = local.cn_tags` single-space sed against an aligned file — mutations need the same precision as checks.
- pack refresh (post-gate deltas, recorded): Q29 now says the residency checks ARE in CI (static) with runtime proof the named gap; Q23, whiteboard 18:30–21:00 + 23:00–25:00 sections, video outline §9, and the residency resume bullet + traceability row all updated to cite E-016. The Phase 8 gate verdict (9.27) was rendered on the pack at 15ce413..7ce09be; these deltas are additive truth-strengthening and are recorded here rather than re-gated — Phase 5's own gate will review the pack again with the phase complete.
- handoff: Phase 5 remaining work listed in ROADMAP; the pack's Q29 gap statement now points at runtime proof as the honest residual. `make residency` is a standing local gate alongside `make plan-sg`.
- stack state: sim stack healthy (6 containers), smoke exit 0 (E-015 capture); no esb-run this session; busforge/helios untouched.

### S2-addendum — 2026-09-10 (same session, post-wrap-up)
- slice: publish the repo + discharge the CI standing obligation
- done: repo created PUBLIC at https://github.com/xenoaitham/silkroute (gh CLI, account xenoaitham); pre-push secrets scan clean (no .env ever committed, only documented sim dummies tracked); pushed main + tag `contract-freeze-erp-v1`; the push triggered the workflow's FIRST-EVER Actions run → GREEN (run 34525438436: sim job 38s, contract job 1m27s) → E-007 + artifact evidence/runs/E-007-first-actions-run.txt; bumped actions checkout/setup-java/upload-artifact v4→v5 (runner deprecation warnings); README CI badge added
- measured: `gh api .../runs/34525438436` → conclusion=success, sha 59088d7; both jobs ✓
- handoff: S3 (Phase 2 ESB) starts with the same 2 deferred contract-suite follow-ups as before (next_actions #1) — unchanged by the GitHub push; future sessions must `git push origin main` at session end
- stack state: sim stack still healthy and running (unchanged)

## Working agreements (from MASTER_PROMPT.md — reminders)
- One session = one phase-slice. Update this file BEFORE committing, at session end, or immediately if context runs low.
- Critic Gate (§5) before any phase is marked DONE. Critic is always a fresh agent.
- Verify before recording: run it yourself, then write the EVIDENCE row. Row claim = reproduce command = artifact, 1:1.
- Never fabricate evidence. An honest "not yet verified" beats an invented green check.
