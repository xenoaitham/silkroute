# SILKROUTE sim-mode environment — copy to .env and adjust if needed.
# These are SIM-ONLY DUMMY credentials for local containers. Never place real secrets in this repo.
# .env is gitignored; this example file is committed.

MYSQL_ROOT_PASSWORD=silkroute
MINIO_ROOT_USER=silkroute
MINIO_ROOT_PASSWORD=silkroute-secret
