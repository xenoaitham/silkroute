# SILKROUTE EVIDENCE LEDGER

The spine of the project. **No claim without a row.** Every row must have a command a stranger can run and a measured result actually observed on this machine. Subagent claims are not evidence — ORCH-LEAD re-runs and records.

Row format:

```
| ID | claim | artifact | reproduce command | measured result |
```

Measurement honesty rules:
- Note the mode (sim/cloud) and the hardware context for perf numbers.
- Never edit a measured number after the fact; supersede with a new row.
- If a claim is currently unverifiable (e.g., no AliCloud account), the row must say so explicitly and point to the honest substitute (e.g., `terraform validate` output).

## Ledger

| ID | claim | artifact | reproduce command | measured result |
|---|---|---|---|---|
| E-001 | Sim-mode infrastructure network starts from a clean state and converges (MySQL 8, Kafka 3.8 KRaft dual-listener, Redis 7, MinIO RELEASE.2025-09-07, Toxiproxy 2.11.0) | docker-compose.yml, Makefile | `make down && make up && make ps && timeout 3 bash -c '</dev/tcp/127.0.0.1/39092'` | 2026-09-08: converged in ~28s — mysql/kafka/redis/minio `(healthy)`, toxiproxy Up with no healthcheck (distroless; readiness = API on :8474). Host binds 127.0.0.1-only; kafka HOST listener accepting on 127.0.0.1:39092 (probe output in artifact tail; as of 2026-09-09 the same probe lives in scripts/smoke.sh so every `make smoke` re-asserts it). Rootless Docker 29.8.0, host potato-linux. See evidence/runs/E-001-compose-ps.txt |
| E-002 | Sim network is reproducible end-to-end from a clean state and ALL 5 services pass authenticated connectivity assertions | docker-compose.yml, scripts/smoke.sh | `make down && make up && make smoke` | 2026-09-08: re-up converged, then `smoke OK (all 5 services) in 2s` — redis PING→PONG; mysqladmin alive + `SELECT VERSION()`=8.0.46 authenticated via container env; kafka broker-api on :9092 (in-container); minio /minio/health/live; toxiproxy /version=2.11.0. See evidence/runs/E-002-smoke.txt |
| E-003 | `make smoke` is falsifiable: it exits non-zero when a service is down (not a always-green check) | scripts/smoke.sh | `docker compose stop redis && make smoke; docker compose start redis && make smoke` | 2026-09-08: with redis stopped → `FAIL: redis PING did not return PONG`, exit 2; after `docker compose start redis` → `smoke OK (all 5 services) in 1s`, exit 0. (The committed artifact is the S2-era re-capture: after host-forward probes were added, a stopped redis fails on the host probe first — `FAIL: redis host forward 127.0.0.1:16379 not reachable` — same exit 2, same restore path; the 2026-09-08 original failed on the PING assertion.) See evidence/runs/E-003-smoke-negative-control.txt |
| E-004 | Phase 1 WSDLs/XSDs validate BEFORE any Java exists: all 7 files xmllint-clean; schemas constrain real payloads (instance validation passes) and reject out-of-enum money (USD ∉ CAD/SGD/CNY) | apps/legacy-erp/src/main/resources/{wsdl,xsd}/ | `xmllint --noout xsd/*.xsd wsdl/*.wsdl && xmllint --noout --schema xsd/pricing-v1.xsd sample.xml` (transcript shows negative case too) | 2026-09-09: 7/7 OK; pricing + inventory instance validation PASS; negative (USD) exit 3 = rejected by schema enum. See evidence/runs/E-004-wsdl-validation.txt |
| E-005 | legacy-erp builds green (17 unit tests incl. xsi:nil marshalling pins) and passes its black-box Karate SOAP 1.2 contract suite — 18 scenarios incl. typed fault paths (UnknownSkuFault, InvalidOrderFault, OutOfStockFault, UnknownReservationFault), duplicate-ref rejection, and auth-failure paths (missing header on BOTH /ws/pricing/v1 and /ws/orders/v1, wrong password, identical-envelope nonce replay rejected by the live server-side cache) — against the frozen WSDLs | apps/legacy-erp/, tests/contract/ | `./mvnw -f apps/legacy-erp/pom.xml clean package && ./mvnw -f tests/contract/pom.xml verify` | 2026-09-11 (S3 refresh; superseded 16-scenario capture of 2026-09-09): unit 17/17; contract 18/18 (pricing 4, orders 6, inventory 4, security 4), BUILD SUCCESS; suite boots the jar itself on 127.0.0.1:18090 (black-box) and hard-fails on missing jar / failed boot (dead-port check proven red by BUILD-QA). See evidence/runs/E-005-contract-tests.txt |
| E-006 | legacy-erp runtime: seeds exactly 50 SKUs / 8 stores (CA×3, SG×3, CN×2) / 400 stock rows, demo generator produces orders through the service layer, and ORCH-LEAD's independent SOAP 1.2 suite (happy + fault + auth + ?wsdl) passes 16/16 | apps/legacy-erp/, scripts/erp-verify.sh | `SERVER_PORT=18080 ERP_DEMO_GENERATE_ORDERS=5 java -jar apps/legacy-erp/target/legacy-erp-*.jar &` then `bash scripts/erp-verify.sh` | 2026-09-09: `ERP SEED: skus=50 stores=8 stockRows=400`; `ERP DEMO: requested=5 submitted=5 skipped=0`; verify PASS=16 FAIL=0 (SGD 490 minor, CAD order totals, SG region in getStock, OutOfStockFault carries availableQuantity, no over-allocation); app stopped + 18080 released after capture. See evidence/runs/E-006-erp-runtime-verification.txt |
| E-007 | CI runs GREEN on GitHub Actions: both jobs (sim compose+smoke via make targets; legacy-erp build 15 unit + Karate 16 contract scenarios) pass on a real ubuntu-latest runner — the "CI is UNVERIFIED" caveat from S1/S2 is discharged | .github/workflows/ci.yml, https://github.com/xenoaitham/silkroute | `gh run list --repo xenoaitham/silkroute --limit 3` (or push any commit to main and watch) | 2026-09-10: run 34525438436 on sha 59088d7, conclusion=success (API-verified) — sim job 38s, contract job 1m27s. First-ever Actions run of this workflow; artifact shows both job results + run URL. See evidence/runs/E-007-first-actions-run.txt |
| E-008 | apps/esb (Camel 4.10.3 LTS + Spring Boot 3.4.5, CXF 4.0.5 client generated from the FROZEN WSDLs) builds green with 33 unit tests, boots loopback-only (REST 18081 / mgmt 18082), and its REST façade POST /api/v1/orders runs the real SOAP saga order→reserve→pricing→confirm through the toxiproxy "erp" proxy with WSS UsernameToken auth: 201 + region-priced exact integer total (CAD 500 minor), 4 COMPLETED steps, per-call attempts, route region; same Idempotency-Key replay → 409 DUPLICATE carrying the original orderId | apps/esb/, scripts/esb-toxiproxy.sh | `make esb-run` then `curl -X POST http://127.0.0.1:18081/api/v1/orders -H 'Content-Type: application/json' -d @order.json` (body per apps/esb/README) | 2026-09-11 (v2, post critic cycle 1): unit 33/33; live 201 AND 409 verbatim in artifact. Two double-encoding bugs found and fixed by ORCH-LEAD (REST body + success events serialized twice) before this capture. See evidence/runs/E-008-esb-build-and-saga.txt |
| E-009 | The ESB resilience behaviors are proven, not claimed, under REAL fault injection (toxiproxy + a deterministic ESB fault hook): retry ladder records attempts=3 under persistent 2500ms latency; retry-after-ambiguous-timeout records attempts=2 with the frozen ERP's ORD-DUP-REF guard holding (no double order); hard-down ERP → CIRCUIT-OPEN fail-fast measured 4ms (<100ms) with DLQ carrying the exhausted correlationIds, then recovery to 201 after the proxy heals; fail-pricing injection → 503 SAGA-COMPENSATED with the inventory hold REALLY released (ERP stock 19→19; releases ride a breaker-free route so compensation works during an OPEN breaker) and the DLQ event carrying compensated:true + pseudonymized customerRef for CN (C1, keyed HMAC); a FAILED saga releases its Redis idempotency claim (same-key retry re-executes — never a 409 lockout); content-based routing visible per store prefix | tests/esb-int/, tests/chaos/esb-faults.sh | `make esb-int` | 2026-09-11 (v2 single-run artifact): Tests run: 8, Failures: 0, Errors: 0 — BUILD SUCCESS (~83s); suite boots its own ERP+ESB and kills by PID files. Single-run transcript + provenance (pid-bound) in evidence/runs/E-009-esb-int-suite.txt and E-009-provenance.txt |
| E-010 | C3 sync SOAP-mediation latency measured with k6: p95 = 44.25ms (< 300ms budget) at 18.63 RPS achieved (20 RPS target), 1304 iterations over 70s, medians 18.25ms — sim mode, everything localhost, consumer desktop (i5-10400F, 12 threads, 16GB, other docker stacks sharing the box) | tests/load/k6-orders.js, tests/load/results/k6-orders-summary.json | `make esb-run && make load-orders && make esb-stop` | 2026-09-11: p95 44.2480541ms; 98.61% checks — the 18 non-201 responses are 422 INV-OUT-OF-STOCK business outcomes from seeded 5..100-unit stock rows legitimately exhausted mid-run (0 infra failures; log grep counts in artifact). See evidence/runs/E-010-k6-run.txt + E-010-k6-summary.json |
| E-011 | CI runs GREEN with the NEW third job: esb saga/retry/DLQ/compensation fault-injection suite (8 scenarios, boots ERP+ESB on the runner) passes on ubuntu-latest alongside the two existing jobs — the timing-sensitive retry/circuit-breaker scenarios hold on a shared CI runner | .github/workflows/ci.yml, tests/esb-int/, https://github.com/xenoaitham/silkroute | `gh run view 34550280537 --repo xenoaitham/silkroute` | 2026-09-11: run 34550280537, sha 214e429, conclusion=success — contract ~87s, sim smoke ~53s, esb suite ~186s, all ✓. See evidence/runs/E-011-first-esb-actions-run.txt |
| E-012 | **validated IaC, sim runtime** — the AliCloud landing zone (infra/: network, security, data, compute=SAE, observability, cn-partition; alicloud provider pinned 1.285.0, terraform 1.16.2) is fmt/validate/plan clean after the SEC-1 fixes AND the critic cycle-1 fixes: 6/6 modules + root validate green (one documented deprecation warning on log_alert.notification_list), `Plan: 79 to add` for the SG hub, `Plan: 110 to add` with `enable_cn_region=true` (CN design proven plan-able, NEVER applied — ADR-0005), CN placement visible in planned values (zone_id/namespace_id `cn-beijing-*`) and enforced at the provider graph via the `alicloud.cn` alias. Plans perform NO AliCloud API calls (placeholder creds, empty local state, zero API-calling data sources). The apply-validity lint (scripts/tf-apply-validity.sh — plan-invisible API rules, born from critic cycle 1's ApsaraMQ topic-naming catch) guards the tree in CI. CI proves the whole job on every push: 4th job `terraform-landing-zone` green | infra/, evidence/runs/E-012-tf-validate-plan.txt, evidence/runs/E-012-first-terraform-ci-run.txt | `make plan-sg` (one command: init+fmt+validate+plan) and `cd infra && ~/tools/terraform/terraform plan -input=false -no-color -var enable_cn_region=true` and `gh run view 34643255075 --repo xenoaitham/silkroute` (green at HEAD, sha 0e5609f; historical first terraform run: `gh run view 34637654107`) | 2026-09-11 (final: post-SEC-1 75/106, post-critic-cycle-1 79/110 — +2 snat_entries, +2 kafka egress rules, +1 network output; earlier captures 73/104 pre-SEC-1). fmt clean, validate Success, lint OK, `make plan-sg` exit 0; CI GREEN AT HEAD: run 34643255075 (sha 0e5609f) 4/4 jobs ✓ after the minio-quay fix; first terraform job run 34637654107 (18s, sha 0e953a2); the two red runs between (34640392548 + rerun) were the Docker Hub tag purge, recorded in the artifact. See evidence/runs/E-012-tf-validate-plan.txt + E-012-first-terraform-ci-run.txt |
| E-013 | SEC-1 independent IAM review of the landing zone returned FIX-FIRST (1 HIGH + 3 MED + 6 LOW) and ALL must-fixes + every actionable LOW were fixed by ORCH-LEAD the same session and re-proven by green plans: HIGH CN-partition region pinning (aliased `alicloud.cn` provider + `providers` meta-argument — was tags-only); MED runtime OSS over-grant (split to artifacts read-only / bronze rw per the data module's own matrix), missing `kms:GenerateDataKey` for SSE-KMS writes, dead CI assume chain (`silkroute-ci` now holds exactly `sts:AssumeRole` on the deploy role); LOWs: TDE + `tde_encryption_key` on BOTH RDS instances, VPC-only `security_ips`, CN Kafka topic residency tags, region-pinned SLS ARNs, trust-boundary/activation notes in infra/README.md. "No wildcard IAM" PASSES under the interpretation recorded verbatim in §1 of the review (zero wildcard Actions; Principal `["*"]` only inside SecureTransport=false Deny statements; Resource wildcards name-pinned or create-time-ID) | compliance/iam-review-phase4.md, infra/security/main.tf, infra/providers.tf | `grep -rnE '"(Action|NotAction)"\s*=\s*\[' infra/ --include='*.tf' -A15 \| grep -cE '"(\*\|[a-z-]+:\*)"'` (expect 0) then `make plan-sg` (re-proves the fixed HCL) | 2026-09-11: zero wildcard actions (SEC-1 grep + re-run); post-fix Plan 75/106 green (E-012 artifact). Review doc itself is point-in-time (pre-fix file:line cites); fixes visible in current code. See evidence/runs/E-012-tf-validate-plan.txt for the post-fix proof |
| E-014 | **validated IaC, sim runtime** — the ~$20 budget alarm exists as an executable, honestly-labeled mechanism: `scripts/budget-alarm.sh` targeting the DOC-VERIFIED BssOpenApi `CreateBudget` API (2023-09-30; BudgetType=CONSUME, Metric=REQUIRE_AMOUNT, WarnConfs — retargeted after critic cycle 1 caught the original draft citing an uncitable `SetBudgets` action), dry-run by default printing the exact payload and performing NO API call; live mode is LIVE=1 + creds. Provider 1.285.0 ships ZERO budget/bss/billing resources (schema-proven, re-run in artifact) so a Terraform budget resource would be fabrication. The billable targets are double-guarded: `make deploy-sg` / `make destroy` REFUSE (exit 2) without SILKROUTE_CLOUD_CONFIRM=YES AND AliCloud credentials — negative paths proven live; positive control `make plan-sg` exits 0 creating nothing | scripts/budget-alarm.sh, Makefile, evidence/runs/E-014-budget-alarm-and-guards.txt | `bash scripts/budget-alarm.sh && make deploy-sg; make destroy; make plan-sg` | 2026-09-11 (v2, post critic cycle 1): schema grep = 0 budget resources; dry-run CreateBudget payload printed; deploy-sg exit 2 REFUSING, destroy exit 2 REFUSING; plan-sg "Plan: 79 to add", exit 0. See evidence/runs/E-014-budget-alarm-and-guards.txt |
| E-015 | Pack-time (S5) re-verification that the two standing ACs still hold today: the sim stack — now SIX services after the S3-era esb-toxiproxy addition — passes its falsifiable smoke, and the one-command landing-zone AC (`make plan-sg`: init + fmt -check + validate + plan, zero API calls) is still green with the same resource count | evidence/runs/E-015-s5-reverification.txt | `make smoke && make plan-sg` | 2026-09-12: `smoke OK (all 6 services, host forwards + in-container) in 2s`, exit 0; fmt clean, validate "Success!" (documented notification_list deprecation warning only), `Plan: 79 to add, 0 to change, 0 to destroy.`, plan-sg exit 0. See evidence/runs/E-015-s5-reverification.txt |
| E-016 | **C1 residency is machine-checked in CI** — `scripts/residency-tests.sh` (seeded from SEC-1 review §5): 7 static checks (R1 provider-graph CN pinning + count flag; R2 CN region strings confined to wiring files + cn-partition; R3 zero cross-region replication/backup/mirror resources; R4 residency tags on every taggable cn-partition resource, schema-tagless types adjudicated; R5 bucket families never cross, management-plane ARN prefixes allowed; R6 no NAT/EIP in the CN partition; R7 single Kafka publish chokepoint wired through PiiMaskingPolicy + saga masks CN customerRef) pass on the real tree, AND the suite is falsifiable: a `--selftest` negative control injects 5 violations (providers meta-argument deleted; replication resource added; KMS tags stripped; bucket family crossed; stray direct Kafka publisher added) and every check must catch its own mutation. Wired into CI (terraform job, `make residency`). Honest label: static graph + wiring proof only — runtime cloud behavior stays verify-at-activation (ADR-0002); behavioral masking proof is E-009. First capture exposed a real bug class: the suite's failure counter lived in a command-substitution subshell, so v1 could never exit non-zero — the selftest caught it before the first commit | scripts/residency-tests.sh, Makefile (`make residency`), .github/workflows/ci.yml (terraform job step), evidence/runs/E-016-residency-tests.txt | `make residency` (or `bash scripts/residency-tests.sh && bash scripts/residency-tests.sh --selftest`) | 2026-09-12: green run `residency OK (7/7 checks)` exit 0; selftest `all five mutations caught` exit 0. See evidence/runs/E-016-residency-tests.txt |
| E-017 | **"Who did what" audit query pattern demonstrated against sim-mode data** — `make audit-demo` (`scripts/audit-demo.sh`) bootstraps the `silkroute_audit.audit_event` table (schema = the DDL committed in compliance/audit-trail-design.md §1) into the sim MySQL, seeds 8 SIMULATED management events (real AliCloud API vocabulary: UpdateRotationPolicy/CreateKey/PutBucketPolicy/StopLogging/DeleteTrail/CreatePolicy/CreateBucket/Decrypt; RFC 5737 documentation IPs), runs the 5 cookbook queries (KMS actors, denied actions, bucket-policy changes, per-identity history, region-anomaly canary), and asserts the reads that matter: 2 denials visible, the StopLogging denial findable, exactly 1 region anomaly, the rotation attributable to a named identity. Falsifiability proven in the artifact: clean bootstrap from DROP DATABASE → exit 0; mysql stopped → exit 2; idempotent re-run (seed skipped) → exit 0; smoke 6/6 after the stop/start cycle. Honest label: sim-mode query-pattern demonstration only — the production store is SLS via the DESIGNED ActionTrail (infra/observability/main.tf), which has never run (ADR-0002); seed rows are simulated | scripts/audit-demo.sh, Makefile (`audit-demo`), compliance/audit-trail-design.md §4, evidence/runs/E-017-audit-demo.txt | `make audit-demo` | 2026-09-13: positive exit 0 (5 queries + assertions held); negative (mysql stopped) exit 2; restore/idempotent re-run exit 0; smoke 6/6 exit 0. SEC-1's independent tamper test (deleted the StopLogging denial row) turned the run red — assertions bite. See evidence/runs/E-017-audit-demo.txt |
| E-018 | CI is green 4/4 at the Phase-5 reviewed HEAD (sha 19ae10c — the compliance layer + all SEC-1/critic fixes included): sim compose+smoke, legacy-erp Karate contract (frozen WSDLs), esb fault-injection suite, and the terraform job (fmt/validate/plan + apply-validity lint + the C1 residency step with selftest) — the "residency tests green in CI" acceptance claim is anchored at the sha the gate reviewed, not two commits back | .github/workflows/ci.yml, evidence/runs/E-018-ci-green-at-phase5-head.txt, https://github.com/xenoaitham/silkroute/actions/runs/34727072981 | `gh run view 34727072981 --repo xenoaitham/silkroute` | 2026-09-13: run 34727072981 @ sha 19ae10c, conclusion=success — esb suite 2m50s, terraform 16s, contract 34s, sim smoke 34s, all ✓. See evidence/runs/E-018-ci-green-at-phase5-head.txt |
| E-019 | **validated IaC, sim runtime** — the Phase-6 SLS alert layer landed and the landing zone re-proved clean: the deprecated `alicloud_log_alert` DLQ alert (ADR-0004 deferred follow-up) migrated to the modern `alicloud_sls_alert` (schema-verified in 1.285.0), joined by three new alerts — C3 p95-budget-breach (`esb-p95-budget-breach`, 300ms threshold), denied-action burst (`audit-denied-action-burst`, >10 non-empty-errorCode events/5min on `audit`, per audit-trail-design §5), and the zero-tolerance trail-tamper tripwire (`audit-trail-tamper-tripwire`, ANY StopLogging/DeleteTrail, STRIDE F-05) — plus 4 `alicloud_log_store_index` resources (the query-facing stores ship their indexes: full-text + request_time/event-json/metric/value fields) and the designed `pipeline-metrics` store (30d; the Phase-3 freshness producer's contract home — labeled awaiting-producer everywhere, incl. the dashboard panel title). Plan-validated both ways with the count delta exactly accounted: **SG `Plan: 87 to add` (was 79), CN `Plan: 118 to add` (was 110); delta +8 = −1 log_alert +4 sls_alert +4 index +1 store**; the plan validator caught one vocabulary divergence live (`group_configuration.type` takes `no_group`, not the legacy `no_condition`) before any doc recorded the wrong form. Notifications route through an SLS action policy that is console-managed at activation (`var.sls_action_policy_id` placeholder — provider 1.285.0 has no action-policy resource, schema-verified); condition-expression SEMANTICS verify at activation (plans prove schema, not SLS behavior). Residency 7/7 + selftest 5/5 and the apply-validity lint re-proven green on the changed tree | infra/observability/ (main.tf, variables.tf, README.md), evidence/runs/E-019-sls-alerts-and-plans.txt | `cd infra && ~/tools/terraform/terraform validate -no-color && ~/tools/terraform/terraform plan -input=false -no-color && ~/tools/terraform/terraform plan -input=false -no-color -var enable_cn_region=true` then `make residency && bash scripts/tf-apply-validity.sh` | 2026-09-13: validate "Success! The configuration is valid."; SG plan exit 0 `Plan: 87 to add, 0 to change, 0 to destroy.`; CN plan exit 0 `Plan: 118 to add`; residency 7/7 + 5/5 selftest; lint OK. See evidence/runs/E-019-sls-alerts-and-plans.txt |
| E-020 | C3 SLO sustained re-proof at 20 RPS (E-010 lineage, unmodified profile + a reporting-only `--summary-trend-stats` flag adding p(99)): p95(all) 131.6 ms / p99(all) 236.2 ms @ 19.49 RPS achieved, 3 704 iterations / 190 s wall (10 s ramp + 180 s hold), 0 network failures; the 352 non-201s verified 1:1 as 422 INV-OUT-OF-STOCK business rejections (ESB-log grep = 352 exactly); budget consumed 44% (p95) / 79% (p99). Honest variance: an identical-shape window the same session measured p95 31.8 ms — 4x swing from shared-host contention alone (hardware note per E-010 precedent: i5-10400F 12T/16GB, other stacks running, load avg 4.4-4.6, sim mode, k6 local not CI) | tests/load/k6-orders.js (unmodified), tests/load/results/s7-sustained-summary.json, evidence/runs/E-020-k6-sustained.txt | `make esb-run && RPS=20 DURATION=180s ~/tools/k6/k6 run --summary-trend-stats="avg,min,med,max,p(90),p(95),p(99)" tests/load/k6-orders.js --summary-export=tests/load/results/s7-sustained-summary.json` (fresh ERP ledger per run) | 2026-09-13: exit 0; see artifact for full summary + non-201 verification + variance note. See evidence/runs/E-020-k6-sustained.txt |
| E-021 | Stepped stress profile finds the C3 breaking point: budget HOLDS through 90 RPS (every step 201-only p95 ≤ ~40 ms, p99 ≤ ~87 ms — cold-start r10 the worst at 39.2/86.2 ms, warm steps ≤ 20.5/24.8 ms — zero infra failures ~11.7k segment-A requests) and at 110 RPS warm-JVM p95 = 266 ms (89% of budget; p99 349 ms = first p99 breach) sustaining 78.9 completed sagas/s; COLD-JVM 110 RPS breaches outright (p95 658 ms) — cold-start + budget-rate spike is a distinct risk signature. Higher rungs unmeasurable: the frozen ERP's seeded stock pool (~7.9k units) drains within the breach window (200 RPS step = zero 201 samples). Infra integrity never broke at any rung (~43.9k requests across A+B+warm-110, zero 5xx/timeouts/resets): saturation on this host = latency blowout, not errors. Per-step percentiles exported via REPORTING thresholds at the 300 ms boundary (no abortOnFail; exit 99 = breach) after live verification that `--summary-export` emits aggregates only (the profile's original per-tag assumption was wrong — corrected in the same session, documented in the profile header) | tests/load/k6-orders-stress.js, tests/load/results/s7-stress-summary.json + s7-stress-segb-summary.json + s7-stress-warm110-summary.json, evidence/runs/E-021-k6-stress.txt | `make esb-run && ~/tools/k6/k6 run --summary-trend-stats="avg,min,med,max,p(90),p(95),p(99)" tests/load/k6-orders-stress.js --summary-export=tests/load/results/s7-stress-summary.json` (segments: STEPS=110,140,170,200 and warm-up + STEPS=110 STEP_DURATION=60 on fresh ERP each) | 2026-09-13: segment A exit 0 (no breach 10-90 RPS); segment B exit 99 (110 RPS cold breach); warm-110 exit 99 (p99 349 ms). Full per-step tables + counters + reading in the artifact. See evidence/runs/E-021-k6-stress.txt |
| E-022 | Chaos H1 (dependency latency DURING load): the hypothesis was first REFUTED and the refutation is the finding — default Spring task executor (8 workers, unbounded queue) turned 800 ms ERP latency × 10 RPS into queue-then-30s-timeout (485 AsyncRequestTimeoutException, k6 p95 pinned at its 30 s cap, 44% checks, achieved 6.3/s) while the ERP calls THEMSELVES succeeded (zero retries, zero breaker activity — a degradation invisible to every resilience mechanism); a Camel thread-pool override did not fix it (wrong pool, re-measured); root cause pinned by thread dump under load (8 `task-N` workers = applicationTaskExecutor). FIX: `spring.task.execution.pool` core 32/max 64/queue 200 (bounded). Post-fix canonical run: H1 HELD — in-window probes med 3.6 s / max 4.1 s (= 4 proxied calls × 800 ms, solo probe 3.22 s vs 15.6 ms clean), zero hangs, zero async timeouts, 14 replays all 409, k6 checks 98.7%; ESB unit 33/33 post-change | tests/chaos/chaos-run.sh (latency-under-load), tests/chaos/transcripts/latency-800ms-*.jsonl, apps/esb/src/main/resources/application.yml, evidence/runs/E-022-chaos-latency.txt | `make esb-run && tests/chaos/chaos-run.sh latency-under-load 10 120 800` | 2026-09-13: pre-fix runs REFUTED (2 runs, both artifacts preserved in E-022's history section); post-fix `analyze: OK — 0 violations`. See evidence/runs/E-022-chaos-latency.txt |
| E-023 | Chaos H2 (ERP hard kill mid-load): HELD first try — ERP killed by PID file (verified dead), kill-window 35/35 probes answered with INSTANT 503s (measured 1.9 ms — the fail-fast signature; body code not captured by the probe parser, so the CIRCUIT-OPEN name is claimed from the 1.9 ms timing + E-009 lineage, not from this run's bodies), retry ladder fired to exhaustion (177 logged attempt failures, Connection reset/EOF), DLQ 0 → 59 with COMPLETE replayable envelopes (field inspection: eventType/externalOrderRef/sourceSystem/storeId/region/customerRef/correlationId/failedStep/attempts{order:3}/errorCode UPSTREAM-UNAVAILABLE/compensated/releasedReservationIds/occurredAt), ERP restarted exactly like make esb-run, health UP at 28 s, FIRST 201 PROBE 2 s AFTER HEALTH (SLO-5 30 s bound), zero hangs, zero duplicate commits (analyze: 0 violations). Honest label: sim pod-kill = java process kill; ERP ledger in-memory (restart re-seeds; cross-restart idempotency via Redis done-keys) | tests/chaos/chaos-run.sh (kill-erp), tests/chaos/transcripts/kill-erp-20260913T063354Z.jsonl, evidence/runs/E-023-chaos-kill-erp.txt | `make esb-run && tests/chaos/chaos-run.sh kill-erp 10 120` | 2026-09-13: `analyze: OK — 0 violations`; recovery 2 s; DLQ delta 59. See evidence/runs/E-023-chaos-kill-erp.txt |
| E-024 | Chaos H3 (Redis outage mid-load): REFUTED as designed, then held after fix — `RedisIdempotencyStore`'s allow-through catch is exception-correct but was unreachable behind Lettuce's 60 s default command timeout: during a 40 s outage every request blocked past its cap, k6 39.65% failed at ~30 s, the FIRST allow-through warn appeared 61 s after outage start (after the heal), post-heal 19× 500 s = queued backlog dying. FIX: `spring.data.redis.timeout`/`connect-timeout` = 500 ms. Post-fix canonical: allow-through engaged 0.9 s into the outage (667 warns), outage-window probes 18/18 completed 201 at med 1.03 s (bounded), replays during the outage returned 422 ORD-DUP-REF — the ERP guard backstop VISIBLY held; honest nuance recorded: post-heal replays of outage-window orders also 422 (done-keys never stored during the outage; `storeCompleted` degrades silently) — the guard held at every layer, no duplicate commit anywhere | tests/chaos/chaos-run.sh (stop-redis), tests/chaos/transcripts/stop-redis-*.jsonl, apps/esb/src/main/resources/application.yml, evidence/runs/E-024-chaos-stop-redis.txt | `make esb-run && tests/chaos/chaos-run.sh stop-redis 10 120` | 2026-09-13: pre-fix `analyze: FAIL — 4 violations` (the measured finding); post-fix `analyze: OK — 0 violations` + `degradation stayed BOUNDED`. See evidence/runs/E-024-chaos-stop-redis.txt |
| E-027 | **Phase-7 acceptance — the README quickstart passes the stranger test from a clean checkout**: with the sim stack torn down first, a fresh `git clone` of sha 37a3f5a walks the README Quickstart top-to-bottom verbatim — `make up && make smoke` converges from nothing (6/6, exit 0), both jars build (17/17, 33/33), `make esb-run` healths, `scripts/demo-order.sh` → 201 (CAD 1000 minor, saga steps + attempts + route), same-key replay → 409 DUPLICATE, latency 800 → re-post 201 in 3.27 s (the E-022 shape), hard-down → 503 UPSTREAM-UNAVAILABLE, clean+heal → 201, `make esb-stop` by PID files, `make plan-sg` → `Plan: 87 to add` — no command needed editing, no divergence | README.md (quickstart), scripts/demo-order.sh, evidence/runs/E-027-stranger-walk.txt | `git clone <repo> /tmp/x && cd /tmp/x` (after `make down` of any running stack) then follow README.md Quickstart top to bottom | 2026-09-13: full walk exit 0 at every step (artifact); walk executed twice this session (first pass caught the fixed-`externalOrderRef` 422 collision — the script now derives the ref from the idempotency key — before the recorded run) |
| E-026 | CI runs GREEN 4/4 at the Phase-6 reviewed HEAD (sha b5f3771 — the E-018 pattern: the gate verdict is anchored at the sha the gate reviewed): sim compose+smoke, legacy-erp Karate contract (frozen WSDLs), esb fault-injection suite, terraform fmt/validate/plan + apply-validity lint + C1 residency step with selftest | .github/workflows/ci.yml, evidence/runs/E-026-ci-green-at-phase6-head.txt, https://github.com/xenoaitham/silkroute/actions/runs/34748892370 | `gh run view 34748892370 --repo xenoaitham/silkroute` | 2026-09-13: run 34748892370 @ sha b5f3771f528d0765772d3d64c97dbf159a399158 (run_number 19), conclusion=success — sim 37s, contract 39s, terraform 17s, esb suite 2m51s, all ✓ (API-verified incl. head_sha). See evidence/runs/E-026-ci-green-at-phase6-head.txt |
| E-025 | Cost model sheet priced ONLY from fetched AliCloud pricing pages: 10 pricing sources fetched 2026-09-13 (SAE CU model w/ SG CU price + coefficients; SLS index/storage/rw rates; OSS Standard LRS; RDS formulas + product-page starter rates; NAT instance+CU; EIP config+traffic; KMS instance model — the MATERIAL FINDING that the design's 2 keys ride a USD 500/month Software KMS Instance since no per-key pricing exists; ApsaraMQ Kafka PostPaid spec/disk/partition rates; CloudMonitor basic free; ActionTrail page body did not render so NO ActionTrail claim made). docs/cost-model.md computes the designed 24/7 SG footprint at 949.64 USD/month mid-assumption (range ≈924-950; dominant: KMS 500 = 53%, Kafka 306 = 32%; first draft 955.39 over-counted the Kafka partition line — post-review re-fetch captured the 1,000 included partitions and the line zeroed, delta recorded in the artifact) with arithmetic inline, assumptions next to numbers, integer minor-units column (C5), and the honest conclusion that this is the quantified argument for sim-first (ADR-0002) + destroy-after-evidence (§8); CN partition section designed-not-running with ADR-0005 prerequisites and NO CN prices invented (none fetched). Counts reconcile to the plan (87 SG = 23 network + 13 security + 8 compute + 25 data + 18 observability; 118 CN = 87+31) | evidence/runs/E-025-cost-pricing-fetches.txt, docs/cost-model.md | `cat evidence/runs/E-025-cost-pricing-fetches.txt` (fetch record) then re-fetch any cited URL to verify the quoted figure; `grep -c "fetched 2026-09-13" docs/cost-model.md` (legend completeness) | 2026-09-13: 10/10 sources recorded; ORCH-LEAD verified DOC-1's arithmetic line-by-line against the fetch notes (SAE 55.42+27.71, Kafka 241.20+64.80; the 5.75 partition line zeroed post-review), then SEC-1 re-derived all 10 lines and re-fetched 7 pricing URLs same-day and the inventory numbers against the IaC (partition_num 12+6, CN delta 31) |
